- **No issue** → `ai-router/session_events.py`, `ai-router/close_session.py`, `ai-router/session_state.py`, `ai-router/docs/close-out.md` §2/§5, `tools/dabbler-ai-orchestration/src/providers/SessionSetsProvider.ts`, `tools/dabbler-ai-orchestration/src/test/suite/forceClosedBadge.test.ts` → The shown implementation satisfies the core hard-scope contract: exact env-var gate (`AI_ROUTER_ALLOW_FORCE_CLOSE_OUT == "1"`), required `--reason-file`, deliberate `closeout_force_used` enum addition with frozen-enum rationale, distinct `WARNING` path via both `outcome.messages` and logger, conditional `forceClosed` plumbing in `mark_session_complete(force=True)`, and null-safe `[FORCED]` badge logic/tests. No fix.

- **Issue** → `tools/dabbler-ai-orchestration/src/fileSystem.ts` (or whatever loads `session-state.json` into `LiveSession`) → The runtime mapping for `forceClosed` is not shown in the deliverables. `types.ts` and `SessionSetsProvider.ts` can compile and unit-test cleanly while the real explorer still never sees the field. **Fix:** map `session-state.json.forceClosed ?? null` into `liveSession.forceClosed` in the loader, and add a loader/integration test so the badge is proven end-to-end instead of only with fake objects.

- **Issue** → `ai-router/docs/close-out.md` combination-rules section/list → `_validate_args` now rejects `--force` with `--interactive`, `--manual-verify`, and `--repair`, but the shown doc changes only cover the §2 summary row and §5 narrative. The acceptance check explicitly asks that the combination-rules list agree. **Fix:** update the doc’s invalid-combination section to match `_validate_args` exactly.

- **Issue** → `ai-router/tests/test_close_session_skeleton.py` → The new rejection tests prove “no events emitted,” but they do not prove “no lock acquired,” which is part of the verification ask for pre-mutation rejection. **Fix:** patch the close-out lock acquisition helper/context manager in the invalid-invocation tests and assert it is never entered when the env var is missing/bad or `--reason-file` is omitted.

- **Issue** → `ai-router/tests/test_close_session_skeleton.py` and `ai-router/tests/test_mark_session_complete_gate.py` → Happy-path coverage is split across two layers: `run()` proves warning + ledger reason, while `mark_session_complete(force=True)` proves `forceClosed`. That leaves no single operator-path test showing `close_session --force` yields all required artifacts together. **Fix:** add one CLI-level force test that drives `run(args)` through a failing-gate scenario and then asserts: exactly one `closeout_force_used` event with `reason`, a `WARNING` message, and `session-state.json.forceClosed is True`.