<!-- routed: task_type=code-review, exclude_providers=['anthropic']; served by gpt-5.5 / gpt-5.5, truncated=False, 233s. Raw, never edited. -->

VERDICT: ISSUES_FOUND

## Findings

1. **Severity:** Major  
   **Category:** false-confidence  
   **Location:** `docs/session-sets/132-session-length-and-explorer-captions/s3-causality-and-compaction.md:194`  
   **Evidence paths:** `docs/session-sets/132-session-length-and-explorer-captions/s3-causality-and-compaction.md`, `docs/session-sets/132-session-length-and-explorer-captions/s3_probe_overhead.py`, `ai_router/spec_admission.py`, `docs/planning/session-set-authoring-guide.md`  
   **Description:** **Violation:** The doc claims per-role totals are “robust to batch logging” because “every member of the batch shares a role” (`s3-causality...md:194-199`). **Impact:** If a same-second batch crosses the work→ceremony boundary, elapsed time is charged entirely to whichever mixed-role mark sorts first, so the reported `F`, `w̄`, and role totals can be wrong. **Evidence:** The probe charges each interval to the current mark’s role (`s3_probe_overhead.py:84-95`); same-timestamp later marks get zero. The skeleton has adjacent work and ceremony steps (`session-set-authoring-guide.md:184-190`), and `classify_steps` assigns roles by position (`spec_admission.py:415-464`). **Fix:** Group marks by timestamp; if a non-zero interval ends at a group containing mixed roles, mark the session un-attributable/drop it from role totals or report it separately, then rerun and update the tables.

2. **Severity:** Major  
   **Category:** false-confidence  
   **Location:** `docs/session-sets/132-session-length-and-explorer-captions/s3-causality-and-compaction.md:200`  
   **Evidence paths:** `docs/session-sets/132-session-length-and-explorer-captions/s3-causality-and-compaction.md`, `docs/session-sets/132-session-length-and-explorer-captions/s3_probe_overhead.py`, `docs/session-sets/132-session-length-and-explorer-captions/s3_probe_tail.py`, `docs/session-sets/132-session-length-and-explorer-captions/s2-measurement.md`  
   **Description:** **Violation:** The document says the overhead population is “97 sessions carrying both a parseable plan and logged marks” and later only discloses that sessions “that logged nothing are invisible” (`s3-causality...md:200`, `:488-489`). **Impact:** The actual filter also drops sessions whose logged step numbers no longer fit the current parse, and the code comment identifies these as D1 parser-defect sessions; that is not neutral selection and can bias the headline measurement. **Evidence:** `s3_probe_overhead.py:75-81` drops any session with an out-of-range mark; `s3_probe_tail.py:188-202` also filters correlation rows to sessions with marks, despite the prose/table framing around same-day population size. `s2-measurement.md:270-277` shows mark coverage is a real subset, not a no-op. **Fix:** Add exclusion counters by reason, disclose the invalid-step-number drop in §3/§8, and either repair those sessions from the logged plan snapshot or report sensitivity with and without them.

3. **Severity:** Major  
   **Category:** false-confidence  
   **Location:** `docs/session-sets/132-session-length-and-explorer-captions/s3-causality-and-compaction.md:19`  
   **Evidence paths:** `docs/session-sets/132-session-length-and-explorer-captions/s3-causality-and-compaction.md`, `docs/session-sets/132-session-length-and-explorer-captions/s2-measurement.md`, `docs/session-sets/132-session-length-and-explorer-captions/s3_probe_overhead.py`  
   **Description:** **Violation:** The doc says the two `F` estimates were “independent” and “share no arithmetic” (`s3-causality...md:19-23`, `:212-215`). **Impact:** The agreement is used as validation for the 40-minute fixed-overhead conclusion, but both estimates inherit the same boundary-write confound that S2 named as D3. **Evidence:** S2 defines duration as `completedAt - startedAt` and says that interval is not effort (`s2-measurement.md:70-72`, `:323-329`). The S3 probe starts at `startedAt` and charges the trailing `completedAt - last_mark` gap to ceremony (`s3_probe_overhead.py:83-98`). **Fix:** Reword as “different formulas over overlapping timestamp sources,” not independent confirmation, and add a sensitivity cut excluding or separately reporting start→first-mark and last-mark→completedAt gaps.

4. **Severity:** Major  
   **Category:** false-confidence  
   **Location:** `docs/session-sets/132-session-length-and-explorer-captions/s3-causality-and-compaction.md:312`  
   **Evidence paths:** `docs/session-sets/132-session-length-and-explorer-captions/s3-causality-and-compaction.md`, `docs/session-sets/132-session-length-and-explorer-captions/s3_probe_tail.py`, `docs/planning/session-set-authoring-guide.md`, `docs/session-sets/132-session-length-and-explorer-captions/change-log.md`  
   **Description:** **Violation:** §8 says the tail finding “cannot orient its own arrow” and the correlations are unmodelled (`s3-causality...md:479-492`), but earlier sections state “The answer ... is: they went round the verification loop more times” and the operator brief says it “found the residual tail is verification loops” (`:312-315`, `:440-445`). **Impact:** An operator reading only the short version/brief could act on a causal claim the evidence does not support. **Evidence:** `s3_probe_tail.py:194-220` computes correlation rankings only; no causal model or multivariate control is implemented. The same overstatement is repeated in the authoring guide (`docs/planning/session-set-authoring-guide.md:301-304`) and change log (`change-log.md:145-149`). **Fix:** Replace causal wording with “verification artifacts are the strongest observed discriminator/candidate explanation; causality is unresolved” in §4, §7, the authoring guide, and the change log.