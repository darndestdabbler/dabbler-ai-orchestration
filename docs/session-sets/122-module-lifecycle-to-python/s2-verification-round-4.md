**VERIFIED** — I checked the current router CLI display path and the fix-delta Playwright hunk. The echo now emits a bare copyable command line, PowerShell-quotes the displayed argv correctly, and the test expectation change matches the current `start_session` register-step behavior.

- Fix verdict: L1 echoed router command copy-pasteability -- fix-accepted
- Fix verdict: L2 echoed command prompt prefix -- fix-accepted