ISSUES FOUND

- **Issue 1:** Tier 4 still does not satisfy the locked “interface surface only” contract for TypeScript/JavaScript; the remediation now includes referenced non-Python files in full.
  - **Category:** Correctness
  - **Severity:** Major
  - **Evidence paths:** `ai_router/context_scope.py:27-33`, `ai_router/context_scope.py:633-658`, `docs/session-sets/138-bounded-context-pull-verification/spec.md:75-78`, `docs/session-sets/138-bounded-context-pull-verification/spec.md:143-147`, `tests/test_context_scope.py:244-270`, `tools/dabbler-ai-orchestration/src/extension.ts:1-13`
  - **Failure scenario:** A normal module-mapped session changes a VS Code extension TypeScript file that imports local helpers, which is common in this repo. `resolve_scope` resolves the imported `.ts` file into tier 4, but emits it as `CONTENT_FULL`, including implementation bodies and comments. That likely inflates or over-budget-refuses scoped reviews for typical TS changes, and it gives the verifier implementation content the locked scope explicitly excludes.
  - **Acceptance criterion:** `JUDGMENT - A referenced TypeScript/JavaScript file in tier 4 must be emitted as parser-derived CONTENT_INTERFACE containing signatures/types/contracts only, with implementation body tokens excluded; tests must fail if the body is carried in full.`
  - **Details:** **Violation:** the spec says tier 4 is “Interface surface only — signatures, types, contracts; no implementation bodies” and Session 1 requires “tier 4 carries interface surface only, extracted by parsing.” **Impact:** this breaks the core bounded-scope invariant for one of the repo’s main languages, so a reasonable reviewer would not accept the scope builder as complete. **Evidence:** `resolve_scope` only calls `interface_surface` for `.py`; every other referenced file is appended as `ScopeEntry(..., CONTENT_FULL, text)`, and the updated test now asserts `CONTENT_FULL` for `src/util/helper.ts`. The dot-prefixed path normalization finding is resolved, but the prior TypeScript surface problem remains in this new form.