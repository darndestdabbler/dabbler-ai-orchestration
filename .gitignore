__pycache__/
*.py[cod]
*.pyc
.venv/
node_modules/
*.vsix
router-metrics.jsonl
# Set 110 S4: the copilot-cli transport's failure log is the same artifact class
# as router-metrics.jsonl above — a per-machine runtime record that churns on
# every failed call and carries no value in version control.
copilot-transport-diagnostics.jsonl
.vscode-test/
.claude/
*.egg-info/
build/
dist/*.tar.gz
dist/*.whl
ai_router/local-overrides.yaml
.session-reasons/
tools/dabbler-ai-orchestration/test-results/
# Set 110 S3: Playwright also writes a repo-root `test-results/.last-run.json`
# when the run is invoked from the repo root rather than from the extension
# directory. Same artifact class as the line above; ignoring only the nested
# path left it showing up as an untracked deliverable in the change set.
test-results/
# Set 029 Session 3 (per-session-set orchestrator marker, schema v3).
# The writer also drops a self-protecting `.gitignore` inside each
# `.dabbler/` directory so consumer repos that haven't patched their
# root `.gitignore` still avoid committing marker files; this entry
# is the workspace-root belt-and-suspenders.
docs/session-sets/*/.dabbler/
# Set 031 (delegation.decision_consensus V1). The per-decision
# JSONL journal (ai_router/consensus-decisions.jsonl) stays
# committed — same precedent as router-metrics.jsonl — so the
# audit trail survives across conversations. The disk-heavy
# full-payload sibling directory (Markdown, one file per call) is
# default-on and explicitly opt-outable via
# delegation.decision_consensus.journal_full_payloads_dir: null.
ai_router/consensus-decisions/
# Set 044 (AI chat-log discovery + experiments). The scratch dir holds
# raw harvested logs / transcripts / sqlite dumps from the discovery
# runs. Lives outside the repo at C:\tmp\dabbler-log-harvest\, but the
# entry below is belt-and-suspenders against an accidental symlink /
# relocation that lands it under the repo root. See
# docs/session-sets/044-.../harvest-objectives-and-redaction.md Part 2.
dabbler-log-harvest/
# Set 060 follow-up (2026-06-11): the extension's tsc test-compile output.
# Regenerated by `npm test` (npx tsc --outDir out) on every run; the
# packaged extension ships from dist/ (esbuild), so out/ is pure derived
# churn. Untracked + ignored to end the recurring dirty-tree noise.
tools/dabbler-ai-orchestration/out/
# Set 078 (Copilot CLI hybrid-tier transport). The seat-local catalog
# lockfile is empirically probed per operator/seat (ai_router/copilot_catalog.py
# --refresh) and never shared truth across seats — same rationale as the
# local-overrides.yaml entry above.
ai_router/copilot-catalog.lock
# Set 109 S3's scrape-to-propose price flow wrote this transient review
# artifact. Set 119 S3 deleted `pricing_proposal`, so nothing produces it
# any more; the entry stays so a stale file left over from that era is
# never committed. A committed proposal would be a stale second opinion
# about live prices.
ai_router/pricing-proposal.json
# Set 124 S1 (operator ruling, 2026-08-12), correcting Set 123: what verifies
# a project is MACHINE/PROJECT state — "this project, on THIS machine" — not
# committed project configuration. It is not machine-wide (two projects on one
# box may legitimately differ) and not project-wide (one project on two boxes
# may legitimately differ: a Copilot seat holds no DABBLER_* keys and must
# resolve COPILOT_CLI, while a teammate who installed the wheel with provider
# keys must resolve DIRECT_API for that same checkout). Committing it would
# force one seat's answer onto every clone — the same mistake, one file over,
# that keeps `transport.profile` out of the packaged router-config.yaml. The
# machine-wide default lives in $AI_ORCHESTRATION_VERIFY_TYPE; the project-wide
# default lives in the committed router-config.yaml; this file is the
# intersection and belongs to neither. Write it with
# `python -m ai_router.verify_type --set <VALUE>`.
#
# ANCHORED to the repo root on purpose. `verify_type.find_project_file` reads
# the project-root copy and nothing else — a nested one is a stale sample that
# can never answer — so an unanchored pattern would buy no safety and would
# silently swallow a fixture some future test means to commit. That is the
# look-alike the Set 124 falsifier plants.
/project-verify-type.txt
