{
  "schemaVersion": 1,
  "sessionNumber": 1,
  "verificationRound": 1,
  "verificationVerdict": "ISSUES_FOUND",
  "issues": [
    {
      "description": "** The TypeScript lazy-synthesis path adds and repeats `spec.md` reads instead of threading one already-read title map.\n  - **Category:** Completeness\n  - **Severity:** Major\n  - **Evidence paths:** `docs/session-sets/115-work-explorer-session-node-ux/spec.md:151-156`, `tools/dabbler-ai-orchestration/src/utils/sessionState.ts:111-120`, `tools/dabbler-ai-orchestration/src/utils/sessionState.ts:155-158`, `tools/dabbler-ai-orchestration/src/utils/progress.ts:106-112`, `tools/dabbler-ai-orchestration/src/utils/fileSystem.ts:953`, `tools/dabbler-ai-orchestration/src/utils/fileSystem.ts:1060-1062`\n  - **Failure scenario:** A normal Work Explorer refresh over a fresh/planned spec-only set now calls `readStatus(dir)`, which infers from disk, then calls `inferStateInMemory(dir)` again for the ledger. Each inference reads `spec.md` once through `specTitleMap()` and again through `readTotalSessionsFromSpec()`. This is probable because spec-only sets are an explicit supported path, and it materially violates the session’s hot-path constraint.\n  - **Acceptance criterion:** `JUDGMENT - For a spec-only set, the TypeScript scan/synthesis path computes the absent-file state once and reuses one parsed/read spec result for both total-session and title resolution, without separate spec-title and total-session file reads or a second inferStateInMemory pass.`\n  - **Details:** **Violation:** the plan says to “Thread the map it already computes into the loop” and “**No new file read** — if the change adds one, it is the wrong change.” **Impact:** this is the exact performance guard for the Work Explorer scan; a reasonable reviewer should block a change that explicitly lands the “wrong change” form. **Evidence:** `notStartedPayload()` calls `specTitleMap(path.join(..., \"spec.md\"))`, whose implementation reads the file, then immediately calls `readTotalSessionsFromSpec()`, which reads the same `spec.md` again; `readSessionSets()` also calls `readStatus(dir)` before separately inferring `rawSd` when the file is absent.",
      "category": "Completeness",
      "severity": "Major",
      "failureScenario": "A normal Work Explorer refresh over a fresh/planned spec-only set now calls `readStatus(dir)`, which infers from disk, then calls `inferStateInMemory(dir)` again for the ledger. Each inference reads `spec.md` once through `specTitleMap()` and again through `readTotalSessionsFromSpec()`. This is probable because spec-only sets are an explicit supported path, and it materially violates the session’s hot-path constraint.",
      "evidencePaths": [
        "docs/session-sets/115-work-explorer-session-node-ux/spec.md",
        "tools/dabbler-ai-orchestration/src/utils/sessionState.ts",
        "tools/dabbler-ai-orchestration/src/utils/progress.ts",
        "tools/dabbler-ai-orchestration/src/utils/fileSystem.ts"
      ],
      "acceptance": {
        "kind": "judgment",
        "statement": "For a spec-only set, the TypeScript scan/synthesis path computes the absent-file state once and reuses one parsed/read spec result for both total-session and title resolution, without separate spec-title and total-session file reads or a second inferStateInMemory pass."
      },
      "discoveryCall": 1,
      "discoveryLens": "spec-conformance"
    },
    {
      "description": "** The TypeScript absent-state path adds and duplicates synchronous `spec.md` reads despite the explicit “No new file read” constraint.\n  - **Category:** Correctness\n  - **Severity:** Major\n  - **Evidence paths:** `tools/dabbler-ai-orchestration/src/utils/sessionState.ts:155`, `tools/dabbler-ai-orchestration/src/utils/sessionState.ts:214`, `tools/dabbler-ai-orchestration/src/utils/progress.ts:106`, `tools/dabbler-ai-orchestration/src/utils/fileSystem.ts:953`, `tools/dabbler-ai-orchestration/src/utils/fileSystem.ts:1060`\n  - **Failure scenario:** A repo has a supported spec-only session set with no `session-state.json`. Every Work Explorer scan calls `readStatus(dir)`, which invokes `inferStateInMemory`, then later calls `inferStateInMemory(dir)` again for the same row; each inference reads `spec.md` once through `specTitleMap` and again through `readTotalSessionsFromSpec`. This is probable because spec-only sets are explicitly supported and tested, and it materially violates the tree-scan performance constraint the plan called out.\n  - **Acceptance criterion:** JUDGMENT - The absent-state TypeScript path derives titles and `totalSessions` from one `spec.md` read per inferred state, and `readSessionSets` reuses that inferred state instead of invoking `inferStateInMemory` twice for the same missing file.\n  - **Details:** **Violation:** the plan says “Thread the map it already computes into the loop… **No new file read — if the change adds one, it is the wrong change.**” **Impact:** the Work Explorer’s supported missing-state branch now performs repeated synchronous disk reads/parses per row, undermining the performance constraint and making the implementation choice merge-relevant. **Evidence:** `notStartedPayload` calls `specTitleMap(...)`, whose extractor reads `spec.md`, then calls `readTotalSessionsFromSpec(...)`, which reads the same file again; `readSessionSets` reaches that inference once through `readStatus` and again when building `rawSd`.",
      "category": "Correctness",
      "severity": "Major",
      "failureScenario": "A repo has a supported spec-only session set with no `session-state.json`. Every Work Explorer scan calls `readStatus(dir)`, which invokes `inferStateInMemory`, then later calls `inferStateInMemory(dir)` again for the same row; each inference reads `spec.md` once through `specTitleMap` and again through `readTotalSessionsFromSpec`. This is probable because spec-only sets are explicitly supported and tested, and it materially violates the tree-scan performance constraint the plan called out.",
      "evidencePaths": [
        "tools/dabbler-ai-orchestration/src/utils/sessionState.ts",
        "tools/dabbler-ai-orchestration/src/utils/progress.ts",
        "tools/dabbler-ai-orchestration/src/utils/fileSystem.ts"
      ],
      "acceptance": {
        "kind": "judgment",
        "statement": "The absent-state TypeScript path derives titles and `totalSessions` from one `spec.md` read per inferred state, and `readSessionSets` reuses that inferred state instead of invoking `inferStateInMemory` twice for the same missing file."
      },
      "discoveryCall": 2,
      "discoveryLens": "failure-scenario"
    }
  ],
  "phase": "discovery",
  "discoveryBaselineTree": "ed44152ead5b675a1a9eb24ce1385028182db934"
}
