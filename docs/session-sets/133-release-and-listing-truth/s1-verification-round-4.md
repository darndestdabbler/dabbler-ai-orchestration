VERIFIED

The fix delta consistently discloses the qualified `DIRECT_API` same-provider exception across the root README, Marketplace README, package description, and changelog while removing unconditional independence claims. No material regression was introduced in the adjacent release surfaces.

Fix verdict: L1 unconditional cross-provider claim conflicted with the same-provider fallback -- fix-accepted  
Fix verdict: L2 -- duplicate-of L1