```json
{
  "tier_model_ok": true,
  "engine_files": "Write all three (AGENTS.md, CLAUDE.md, GEMINI.md) for BOTH tiers, always. This prevents orchestrator-lock-in and drift. Each must be a thin pointer to a canonical URL in dabbler-ai-orchestration. The pointed-to document MUST instruct the cold orchestrator to first read spec.md to find its `tier:`, then execute the corresponding Python lifecycle. For Lightweight, it must explicitly state: 'You are in tier:lightweight. You will NOT use the AI router for API calls. You WILL use the Python session lifecycle (start_session, close_session) which will guide you through manual verification.' This instruction is non-negotiable for cold-start self-orientation.",
  "setup_uniformity": "YES. Both tiers MUST scaffold a Python venv and `pip install dabbler-ai-router`. This is the core of the fix. The ONLY divergence is that the 'Full' setup path writes router configuration files and sets `tier: full` in spec.md, while the 'Lightweight' path writes no router config and sets `tier: lightweight`. This makes the tier a declarative switch on a uniform foundation, eliminating the drift and confusion that caused the initial bug.",
  "spec_template": "The canonical spec.md template must contain these required, non-empty fields: `sessionSetSlug: 'NNN-some-slug'`, `schemaVersion: 4`, `tier: 'full' | 'lightweight'`, `verificationMode: 'dedicated-sessions' | 'out-of-band-or-none'`. The `sessionGenPrompt.ts` generator command MUST be fixed to use this template, enforce the `NNN-` numeric prefix on the slug, and prompt the user for tier and verificationMode or use documented, safe defaults (`tier: lightweight`, `verificationMode: out-of-band-or-none`).",
  "cold_start": "The orchestrator's entrypoint is its engine file (e.g., CLAUDE.md). This file must contain an immutable bootstrap instruction: 'Your first action in any session is to read `docs/session-sets/<current-slug>/spec.md`.' The orchestrator parses `spec.md`, reads the `tier:` key, and knows its operational mode (router on/off) before executing `start_session`. This `spec.md`-as-mission-briefing model is the only reliable mechanism for a cold orchestrator to self-orient.",
  "fix_location": "BOTH. The fix is incomplete otherwise. The deterministic extension CODE (`gitScaffold.ts`, `sessionGenPrompt.ts`, `wizard.html` logic) must be updated to scaffold correctly. This makes the system functional and drift-resistant. The DOC+canonical-template (`adoption-bootstrap.md`, `README.md`, wizard UI text) must be updated to explain the model correctly. This makes the system understandable. Prioritize the code fix; a correct but poorly documented system is better than a broken but well-documented one. Ship a Marketplace release.",
  "doc_reconciliation": "Single Source of Truth (SSoT). Create a new canonical document, `docs/concepts/TIER_MODEL.md`, in the main repository that is the definitive explanation of the Full and Lightweight tiers. All other documents (README, adoption-bootstrap.md, the Get Started wizard, the document pointed to by engine files) must be refactored to contain only a brief summary and a direct, stable link to this SSoT file. This SSoT-and-pointer architecture prevents drift.",
  "sequencing": "1. Establish Truth: Write the canonical `docs/concepts/TIER_MODEL.md` and the `spec.md` template. 2. Fix the Code: Update the VS Code extension commands and wizard generator to produce correct, uniform scaffolding for both tiers. 3. Fix Human Docs: Update README, adoption bootstrap, and wizard UI text to point to the SSoT. 4. Fix Orchestrator Docs: Update the canonical engine-file content with the cold-start instruction. 5. Release & Verify: Ship the extension and test the end-to-end scaffolding and cold-start execution for both tiers.",
  "confidence": "high",
  "key_risks": "The primary risk is a partial fix. If the code generators are not updated, the fast path will continue to create broken repos, invalidating all documentation changes. The secondary risk is a slow VS Code Marketplace release cycle delaying the code fix, creating a window where docs and reality are misaligned.",
  "strongest_dissent": "The best argument against this is that it forces Python lifecycle overhead (venv, pip install) onto Lightweight users who may have wanted a 'pure documentation' workflow. This position argues that our 'uniformity' is actually just complexity inflicted on the simple use case. My rebuttal: the 'simple' model was an illusion that was already broken and inconsistent with the implemented `--no-router` mode. A single, predictable lifecycle with one declarative switch is fundamentally more robust and less confusing than two divergent lifecycles that have already proven their tendency to drift into a failure state."
}
```