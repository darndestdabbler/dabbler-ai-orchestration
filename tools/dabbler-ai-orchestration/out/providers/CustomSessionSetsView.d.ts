import * as vscode from "vscode";
import { ScanState } from "./scanState";
import { isCurrentSessionInFlight } from "./SessionSetsModel";
export declare class CustomSessionSetsView implements vscode.WebviewViewProvider, vscode.Disposable {
    private readonly context;
    private readonly scanState;
    static readonly viewType = "dabblerSessionSets";
    private view;
    private version;
    private renderTimer;
    private cache;
    private welcomeHtml;
    private readonly harvest;
    constructor(context: vscode.ExtensionContext, scanState: ScanState);
    dispose(): void;
    refresh(): void;
    resolveWebviewView(webviewView: vscode.WebviewView, _context: vscode.WebviewViewResolveContext, _token: vscode.CancellationToken): void;
    private onMessage;
    private dispatchCommand;
    private showContextMenu;
    private readSupports;
    private findSetBySlug;
    private getSuppression;
    private setSuppression;
    private handleToggle;
    private scheduleRender;
    private postSnapshot;
    private postScanState;
    private postSuppressionEcho;
    private toProtocolScanState;
    private buildBuckets;
    private buildBucket;
    private buildRow;
    private loadWelcomeHtmlFromPackageJson;
    private renderWelcomeMarkdown;
    private renderShell;
    private escHtml;
    private escAttr;
}
export { isCurrentSessionInFlight };
