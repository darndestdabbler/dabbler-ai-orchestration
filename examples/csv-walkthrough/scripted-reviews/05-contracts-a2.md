VERIFIED

I checked only the change. `csv-app` no longer promises wording it does not
own; it promises pass-through, which is a promise about the app and is testable
without pinning any text. The test was renamed to match what it now proves,
rather than left naming the old promise. The "not promised" entry states the
reasoning, so the next person to read it will not re-introduce the coupling.
