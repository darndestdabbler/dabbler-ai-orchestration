# Cancellation history

Cancelled on 2026-08-19T03:39:09-04:00
Cancelled during session 3 on the set's own measurement. Two premises failed. (1) Billing: the spec rejected an agentic pull because it 'bills per turn', but Copilot and the direct APIs bill per TOKEN, so bundle size is the dominant cost and the architecture optimises the wrong variable -- the same review cost 121,820 chars scoped vs 16,233 monolithic (~7.5x, every round, forever). (2) Contracts beat review: the planted cross-file defect is caught by the existing pytest suite in 7 seconds at zero marginal cost, and is not even expressible against a keyword-only API. The correctness finding stands and is preserved -- with no ambient filesystem access the same verifier returned Major from the scoped bundle and VERIFIED from the monolithic one, on both corpora -- but the variable that matters is whether a repo has enforceable contracts, not scoped-vs-monolithic. Sessions 1 and 2 remain committed and dormant on main pending a redesign discussion. Full evidence, the measurement, and the reusable fixes (session log subcommand, stale-doc corrections, stem-symbol fix, ScopeManifestError) are salvaged outside the repo at .copilot/session-state/350c17da-bf29-422c-93ab-b828baf275db/files/set-138-salvage/.

