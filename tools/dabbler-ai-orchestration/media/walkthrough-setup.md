# Set the project up

One command writes every file the workflow needs and commits them:

- the managed guidance the AI engines read,
- the ignore rule for the router machine-side state,
- `dabbler.yaml`, this project declaring how its tests run,
- and the first two sessions.

Nothing is installed. The router ships inside this extension.
