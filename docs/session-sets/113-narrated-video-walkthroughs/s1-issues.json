{
  "schemaVersion": 1,
  "sessionNumber": 1,
  "verificationRound": 1,
  "verificationVerdict": "ISSUES_FOUND",
  "issues": [
    {
      "description": "** Valid indentationless YAML sequences are parsed as an empty inventory, allowing declared components to close with no records.\n  - **Category:** Correctness\n  - **Severity:** Critical\n  - **Evidence paths:** `ai_router/spec_config.py`, `ai_router/gate_checks.py`, `ai_router/tests/test_set113_uat_accounting.py`\n  - **Failure scenario:** A consumer or YAML formatter writes the valid YAML form `uatComponents:\\n- Work Explorer\\n- Static index`. `_parse_uat_components()` stops because each item’s indentation is equal to the key’s indentation and returns `()`. The gate then treats the inventory as explicitly empty, and the documented empty `disposition.uat.components` accounting passes. Indentationless sequences are common output from YAML tooling, so this is a probable real authoring shape, not an adversarial edge case.\n  - **Acceptance criterion:** `python -c \"exec(\\\"import runpy\\\\nm = runpy.run_path('ai_router/spec_config.py')\\\\nassert m['_parse_uat_components']('uatComponents:\\\\n- Work Explorer\\\\n- Static index\\\\n') == ('Work Explorer', 'Static index')\\\")\"`\n  - **Acceptance expectation:** exit 0\n  - **Details:** **Violation:** “Every declared in-scope component must carry a record” and “a session cannot close having silently ignored a component.” **Impact:** The central inventory boundary fails open: two declared components become an empty inventory and can close with no component records, directly defeating the session objective. **Evidence:** `_parse_uat_components()` breaks when item indentation is `<= key_indent`; `check_uat_walk_recorded()` explicitly accepts an empty inventory paired with an empty accounting. The parser must recognize YAML’s legal indentationless sequence form or explicitly reject it rather than converting it to `()`.",
      "category": "Correctness",
      "severity": "Critical",
      "failureScenario": "A consumer or YAML formatter writes the valid YAML form `uatComponents:\\n- Work Explorer\\n- Static index`. `_parse_uat_components()` stops because each item’s indentation is equal to the key’s indentation and returns `()`. The gate then treats the inventory as explicitly empty, and the documented empty `disposition.uat.components` accounting passes. Indentationless sequences are common output from YAML tooling, so this is a probable real authoring shape, not an adversarial edge case.",
      "evidencePaths": [
        "ai_router/spec_config.py",
        "ai_router/gate_checks.py",
        "ai_router/tests/test_set113_uat_accounting.py"
      ],
      "acceptance": {
        "kind": "executable",
        "command": "python -c \"exec(\\\"import runpy\\\\nm = runpy.run_path('ai_router/spec_config.py')\\\\nassert m['_parse_uat_components']('uatComponents:\\\\n- Work Explorer\\\\n- Static index\\\\n') == ('Work Explorer', 'Static index')\\\")\"",
        "expectedExitCode": 0
      },
      "discoveryCall": 1,
      "discoveryLens": "spec-conformance"
    },
    {
      "description": "** The documented `uatComponents` example is misparsed because trailing YAML comments become part of component names.\n  - **Category:** Correctness\n  - **Severity:** Major\n  - **Evidence paths:** `ai_router/spec_config.py`, `ai_router/gate_checks.py`, `docs/planning/session-set-authoring-guide.md`\n  - **Failure scenario:** An author copies the guide’s configuration, including `- Work Explorer tree   # in-scope components...`, then records component `\"Work Explorer tree\"` as shown by the prose. `_LIST_ITEM_RE` retains the comment in the inventory value, so the gate reports the clean component as missing/extra and blocks close. This is probable because the repository’s canonical authoring example itself uses trailing comments.\n  - **Acceptance criterion:** `python -c \"exec(\\\"import runpy\\\\nm = runpy.run_path('ai_router/spec_config.py')\\\\nassert m['_parse_uat_components']('uatComponents:\\\\n  - Work Explorer tree # human-observable surface\\\\n') == ('Work Explorer tree',)\\\")\"`\n  - **Acceptance expectation:** exit 0\n  - **Details:** **Violation:** The parser claims to support a hand-written YAML block inventory, while the authoring guide presents trailing comments as valid usage. **Impact:** Following the official migration guidance produces an unsatisfiable or misleading inventory and blocks a typical consumer at close. **Evidence:** `_LIST_ITEM_RE` captures the complete remainder of the list-item line and `_strip_quotes()` does not remove YAML comments, while the guide places comments directly after both example values. Comments must be stripped outside quoted scalars, or the guide must use syntax the parser actually supports.",
      "category": "Correctness",
      "severity": "Major",
      "failureScenario": "An author copies the guide’s configuration, including `- Work Explorer tree   # in-scope components...`, then records component `\"Work Explorer tree\"` as shown by the prose. `_LIST_ITEM_RE` retains the comment in the inventory value, so the gate reports the clean component as missing/extra and blocks close. This is probable because the repository’s canonical authoring example itself uses trailing comments.",
      "evidencePaths": [
        "ai_router/spec_config.py",
        "ai_router/gate_checks.py",
        "docs/planning/session-set-authoring-guide.md"
      ],
      "acceptance": {
        "kind": "executable",
        "command": "python -c \"exec(\\\"import runpy\\\\nm = runpy.run_path('ai_router/spec_config.py')\\\\nassert m['_parse_uat_components']('uatComponents:\\\\n  - Work Explorer tree # human-observable surface\\\\n') == ('Work Explorer tree',)\\\")\"",
        "expectedExitCode": 0
      },
      "discoveryCall": 1,
      "discoveryLens": "spec-conformance"
    },
    {
      "description": "** The Python validator accepts unknown top-level `uat` fields that the JSON schema rejects.\n  - **Category:** Correctness\n  - **Severity:** Major\n  - **Evidence paths:** `ai_router/disposition.py`, `ai_router/schemas/disposition.schema.json`, `docs/disposition-schema.md`\n  - **Failure scenario:** A consumer migrating from Set 111 leaves `walkArtifact` at the `uat` top level after adding the new component accounting, or adds a session-wide `confidence` field. `validate_disposition()` accepts the block, while `disposition.schema.json` rejects it under `additionalProperties: false`. Stale `walkArtifact` is a probable migration residue for this explicitly breaking release, so users of the router validator and users of the published schema receive contradictory answers.\n  - **Acceptance criterion:** `python -c \"exec(\\\"import runpy\\\\nm = runpy.run_path('ai_router/disposition.py')\\\\nerrors = m['_validate_uat_block']({'attestation': 'a', 'components': [], 'confidence': 0.8})\\\\nassert errors\\\")\"`\n  - **Acceptance expectation:** exit 0\n  - **Details:** **Violation:** The work promises a facts-only record and validator/schema parity; the schema closes the top-level object to `attestation` and `components`. **Impact:** Forbidden metadata can pass the router’s Python validation while failing schema-based tooling, and a confidence score can still be recorded one level above the closed component object. **Evidence:** `_validate_uat_block()` special-cases `status` but never checks the remaining top-level key set, whereas the schema sets `additionalProperties: false`. The Python validator must reject every unknown top-level `uat` key, retaining the tailored migration message for `status`.",
      "category": "Correctness",
      "severity": "Major",
      "failureScenario": "A consumer migrating from Set 111 leaves `walkArtifact` at the `uat` top level after adding the new component accounting, or adds a session-wide `confidence` field. `validate_disposition()` accepts the block, while `disposition.schema.json` rejects it under `additionalProperties: false`. Stale `walkArtifact` is a probable migration residue for this explicitly breaking release, so users of the router validator and users of the published schema receive contradictory answers.",
      "evidencePaths": [
        "ai_router/disposition.py",
        "ai_router/schemas/disposition.schema.json",
        "docs/disposition-schema.md"
      ],
      "acceptance": {
        "kind": "executable",
        "command": "python -c \"exec(\\\"import runpy\\\\nm = runpy.run_path('ai_router/disposition.py')\\\\nerrors = m['_validate_uat_block']({'attestation': 'a', 'components': [], 'confidence': 0.8})\\\\nassert errors\\\")\"",
        "expectedExitCode": 0
      },
      "discoveryCall": 1,
      "discoveryLens": "spec-conformance"
    },
    {
      "description": "The inventory parser silently converts valid, non-empty YAML inventories into partial or empty inventories**\n  - **Category:** Correctness\n  - **Severity:** Critical\n  - **Evidence paths:** `ai_router/spec_config.py, ai_router/gate_checks.py, ai_router/tests/test_set113_uat_accounting.py`\n  - **Failure scenario:** A spec author places a YAML comment before or between `uatComponents` entries—common in hand-authored configuration. `_parse_uat_components()` stops at the first comment-only line and returns only the entries seen before it, or `()` if the comment precedes the first entry. `check_uat_walk_recorded()` then treats that truncated tuple as the complete declared inventory. A disposition can therefore omit every component after the comment, or use `components: []`, and close successfully. This is probable because the configuration format and authoring guide encourage inline explanatory comments, while YAML permits comments between sequence entries.\n  - **Acceptance criterion:** JUDGMENT - Parsing a canonical configuration containing comment-only lines before and between `uatComponents` items must preserve every item, and the close gate must refuse a disposition omitting any of them; a bare/null `uatComponents:` must not be interpreted as the explicit empty list `uatComponents: []`.\n  - **Details:** **Violation:** The session requires that “Every declared in-scope component must carry a record,” and identifies any path allowing a declared component to close without one as Critical. In `_parse_uat_components()`, any nonblank line not matching `_LIST_ITEM_RE` immediately terminates the scan; a YAML comment is such a line. The same function also returns `()` for a bare `uatComponents:` even though the stated contract says `uatComponents: []` is the explicit empty form. **Impact:** The load-bearing inventory can be silently under-read, directly restoring component evaporation and changing the merge decision. **Evidence:** `gate_checks.py` trusts the resulting tuple and considers an empty inventory fully covered by an empty accounting. The new tests cover ordinary lists and adjacent keys but do not cover comments, truncation, or bare/null values. **Fix:** Parse the YAML sequence without terminating on comment-only lines, distinguish explicit `[]` from null/malformed declarations, and fail closed on unsupported syntax rather than returning a smaller inventory.",
      "category": "Correctness",
      "severity": "Critical",
      "failureScenario": "A spec author places a YAML comment before or between `uatComponents` entries—common in hand-authored configuration. `_parse_uat_components()` stops at the first comment-only line and returns only the entries seen before it, or `()` if the comment precedes the first entry. `check_uat_walk_recorded()` then treats that truncated tuple as the complete declared inventory. A disposition can therefore omit every component after the comment, or use `components: []`, and close successfully. This is probable because the configuration format and authoring guide encourage inline explanatory comments, while YAML permits comments between sequence entries.",
      "evidencePaths": [
        "ai_router/spec_config.py",
        "ai_router/gate_checks.py",
        "ai_router/tests/test_set113_uat_accounting.py"
      ],
      "acceptance": {
        "kind": "judgment",
        "statement": "Parsing a canonical configuration containing comment-only lines before and between `uatComponents` items must preserve every item, and the close gate must refuse a disposition omitting any of them; a bare/null `uatComponents:` must not be interpreted as the explicit empty list `uatComponents: []`."
      },
      "discoveryCall": 2,
      "discoveryLens": "failure-scenario"
    }
  ],
  "phase": "discovery",
  "discoveryBaselineTree": "9dbcfe3c74b784ba852acaaee5290659070cf16d"
}
