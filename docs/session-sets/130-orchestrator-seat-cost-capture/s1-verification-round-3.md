**VERIFIED** — I checked L1 against the actual identifier surfaces: `seat_cost.py` constants/report serialization, `seat-cost.md`, the close-out pointer, the session spec, and the new pinning tests. The fix consistently uses `orchestrator_seat`, `routed_seat`, and `routed_api`; I found no in-delta blocking defect.

- Fix verdict: L1 public component identifiers mismatch -- fix-accepted