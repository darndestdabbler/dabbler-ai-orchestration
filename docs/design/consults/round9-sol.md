# Design consult, round 9: Sol reviews the session plan for sessions 100–108

model_name: gpt-5-6-sol
model_id: gpt-5.6-sol
provider: openai
tokens: 10158 in / 12996 out
elapsed: 226.626s  truncated: false
---
1. **Issue →** Fresh single-module repositories would receive `docs/modules.yaml`, a new solution-plan deliverable, and module-cut questions, violating the absent-manifest behaviour required here. **Location →** Session 100, step 3, changing `bootstrap/index.ts` and `bootstrap/templates.ts`; the plan states bootstrap currently writes `solution.yaml` and two setup sessions. **Fix →** Write instead: “For an absent or one-entry manifest, bootstrap preserves today’s files, prompts, and `docs/planning/project-plan.md` flow and does not create `docs/modules.yaml`. Module planning is opt-in through `dabbler modules create`; only acceptance of a second valid entry activates `docs/planning/solution-plan.md` and the module-aware decomposition template.”

2. **Issue →** Deleting the `workflow` and `solution` verbs, `solution.yaml` scaffold, implementation, and existing tests would remove behaviour from every absent-manifest repository rather than merely disabling it for multi-module mode. **Location →** Session 100, step 4: `solution.ts`, `cli/workflow.ts`, `cli/solution.ts`, `workflow/commands.ts`, `workflow/log.ts`, `workflow/terminal.ts`, `stepreview.ts`, `testphase.ts`, `fixloop.ts`, their five tests, `cli/registry.ts`, and `contracts/verbs.ts`. **Fix →** Write instead: “`solutionShape` dispatches absent/one-entry repositories through a compatibility implementation that preserves the current verbs, scaffold, outputs, and tests; repositories with more than one manifest entry use the module workflow and projection. Files may be consolidated only after `solution.test.ts`, `workflow.test.ts`, `stepreview.test.ts`, `testphase.test.ts`, and `fixloop.test.ts` pass unchanged against that compatibility surface.”

3. **Issue →** Module metadata would leak into single-module records and contract enforcement could activate for an explicitly declared one-entry manifest, despite the requirement that one declared module still has no module-specific obligations. **Location →** Session 101, steps 2–4, and Session 102, steps 2–4, including `sessions.schema.json`, `contractdoc.ts`, and derived suite `covers`. **Fix →** Write instead: “When `solutionShape` is single, do not persist or print `modules` on the session record, do not interpret module suite roles or module packaging blocks, preserve `contractdoc.ts`’s existing standalone behaviour, and impose no `modules/<slug>/contract/` path, scaffold, refusal, or derived freshness input. These behaviours activate only for a shape with more than one entry.”

4. **Issue →** Importing `.dabbler/overlay.targets` from `Directory.Build.props` occurs before project `PackageReference` items are declared, so removing them and adding `ProjectReference` items there is unreliable. **Location →** Session 103, step 2, and Session 105, step 5. **Fix →** Write instead: “Keep driven properties such as `EnableSourceLink` in `Directory.Build.props`; add a tracked `Directory.Build.targets` that conditionally imports the untracked `.dabbler/overlay.targets` only when `DABBLER_DRIVEN` is set and the file exists. The overlay runs after project items exist, removes the mapped `PackageReference`, and adds the corresponding `ProjectReference`, conditioned per consuming project.”

5. **Issue →** Custom packaging cannot receive the generated version because `packaging.ts` currently substitutes only `{output}`, `{artifact}`, `{feed}`, and `{secret}`; additionally, a hexadecimal digest does not guarantee that a changed tree sorts after its predecessor under NuGet precedence, and `Directory.Packages.props` provides one effective central pin rather than a pin per consumer. **Location →** Session 101, step 4; Session 103, step 3; and Session 107, step 3. **Fix →** Write instead: “Add `{version}` to `loadDeclaration`’s supported substitutions. Form the version from a release `VersionPrefix` as `<base>-dev.<yyyymmdd>.<monotonic-numeric-build>.g<digest>`, storing the build allocation so retries are stable; do not claim digest order. Update one unambiguous effective `PackageVersion` entry while preserving the XML structure, refuse ambiguous conditioned entries, require consuming `PackageReference` items to omit `Version` and `VersionOverride`, and make `pins_current` validate that central declaration.”

6. **Issue →** The correspondence record cannot contain the producing commit before `phaseLand`, because `drive.ts` creates that commit only after the package and record are already in the tree; embedding the eventual commit would create a self-reference. **Location →** Session 103, step 3, and Session 107, step 2. **Fix →** Write instead: “The committed correspondence record contains the source digest, contract digest, candidate version, session identity, and pre-land base commit. After `phaseLand` creates the commit, its existing land/gate receipt records the resulting commit and maps it to that correspondence record; no tracked file claims the final commit before it exists.”

7. **Issue →** `package` as one string and `contract: abstractions` are .NET vocabulary and cannot represent Maven coordinates or its API artifact cleanly. **Location →** Session 100, step 2; Session 102, steps 2–3; and Session 108, step 2. **Fix →** Write instead: “Use `artifact: {type: "nuget", id: string} | {type: "maven", groupId: string, artifactId: string}` and `contract: "api" | "artifact" | "generated"`. Use `artifact`, `API project`, `binary dependency`, and `source dependency` throughout commands, projection data, bundle records, and grant logic; ecosystem adapters translate those terms to `PackageReference`/`ProjectReference` or Maven coordinates/reactor projects.”

8. **Issue →** Session 108 retrofits six seams created across Sessions 102–105, adds Maven packaging and scaffolding, and writes a design document in one run; moreover, Maven does not load an arbitrary `.mvn/dabbler-overlay.xml` as an external profile. **Location →** Session 108, steps 2–3, referring back to Sessions 102 step 3, 103 steps 2–3, 104 step 3, and 105 step 5. **Fix →** Write instead: “Introduce `ecosystem.ts` before Session 102 and implement Maven beside .NET in each originating session: contracts in 102, repository-layout packaging in 103, focused invocation in 104, and grants in 105. Maven commands use the repository’s declared reactor build plus an explicitly passed generated settings file for the committed file repository; source grants invoke a declared reactor-compatible command or refuse with setup required, rather than synthesizing a parent POM or external profile. Session 108 retains only cross-ecosystem integration checks and `docs/design/hardened-profiles.md`.”

### Cuts

Cut Session 108 step 3 first; `docs/design/hardened-profiles.md` is not on the execution path proven by `docs/design/module-checkout-poc.md`.  
Cut Session 106 step 4 and Session 107 step 4 next; their Solution Explorer visualizations do not implement the router gates.  
Never cut the absent/one-entry `solutionShape` compatibility required in Session 100.  
Never cut focused checkout and verifier enforcement in Sessions 104–105, selected `final-full` evidence in 106, or tested-bytes land checks in 107.
