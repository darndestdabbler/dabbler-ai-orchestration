# ============================================================
# AI Router Configuration (v2)
# ============================================================
# All model info, roles, thresholds, and behavior in one file.
# API keys are NEVER stored here — providers name an env var and
# values resolve through ai_router.secret_resolver at call time.
# Dollars are not computed anywhere: tokens are recorded, and
# reconciliation happens against the vendor's own console.

providers:
  anthropic:
    display_label: Anthropic
    enabled: true
    api_key_env: DABBLER_ANTHROPIC_API_KEY
    base_url: https://api.anthropic.com/v1/messages
    api_version: "2023-06-01"
    rate_limit:
      requests_per_minute: 50
      tokens_per_minute: 80000
    timeout_seconds: 600
    retry:
      max_retries: 2
      backoff_base_seconds: 2

  google:
    display_label: Google
    enabled: true
    api_key_env: DABBLER_GEMINI_API_KEY
    base_url: https://generativelanguage.googleapis.com/v1beta
    rate_limit:
      requests_per_minute: 60
      tokens_per_minute: 100000
    # 900, not 300: a tier-3 model reasoning at high effort over a large
    # evidence bundle routinely exceeds 300s, and a timeout on a mandatory
    # verification call is a correctness failure, not a comfort issue.
    timeout_seconds: 900
    retry:
      max_retries: 2
      backoff_base_seconds: 2

  openai:
    display_label: OpenAI
    enabled: true
    api_key_env: DABBLER_OPENAI_API_KEY
    base_url: https://api.openai.com/v1
    rate_limit:
      requests_per_minute: 60
      tokens_per_minute: 100000
    timeout_seconds: 900
    retry:
      max_retries: 2
      backoff_base_seconds: 2

# Transport for routine dispatch. Resolution precedence: --transport CLI
# flag > DABBLER_TRANSPORT env var > this value > default "api". The seat is
# the default because it is the surface staff receive; the direct-API path
# stays fully supported and merely stops being what you get by not choosing.
# A machine without a seat says so in a project-local local-overrides.yaml.
transport:
  profile: copilot-cli

# Selection is by role, and one resolver applies it on both transports
# (ai_router.selection). A role declares the provider set it may draw from —
# a hard filter — and a preference order, which is ordering only: a model the
# order does not name still qualifies and simply sorts after the named ones.
# That is what keeps a stale list from costing a candidate. Names are model
# ids as each transport puts them on the wire, so a name that exists on only
# one path is inert on the other rather than an error.
roles:
  generator:
    prefer: [claude-sonnet-4.6, gpt-5.5, gemini-3.1-pro-preview]
    require_provider_in: [anthropic, openai, google]
  verifier:
    # A verifier's job is to be right about whether work ships, which is the
    # wrong place to economise. Entries the registry marks
    # is_enabled_as_verifier: false are refused on both transports, so this
    # order never names one.
    prefer: [gpt-5.6-sol, gpt-5.4, claude-sonnet-4.6]
    require_provider_in: [anthropic, openai, google]
  plan-review:
    prefer: [gpt-5.4-mini, gemini-2.5-flash, claude-haiku-4.5]
    require_provider_in: [anthropic, openai, google]
  plan-review-escalated:
    prefer: [claude-opus-5, gpt-5.5, gemini-3.1-pro-preview]
    require_provider_in: [anthropic, openai, google]

# The copilot-cli transport's own settings; inert unless that transport is
# selected. The catalog lockfile is model-truth for this seat; the roles above
# are intent.
transports:
  copilot-cli:
    binary: copilot
    lockfile: copilot-catalog.lock   # relative to this file's directory
    billed_usage_unavailable: true    # honest non-accounting; seat spend is measured by ai_router.seat_cost
    max_invocations_per_session: 200  # per-process hard circuit breaker, not a budget
    # Dispatch ceilings; must satisfy spawn < first_byte < total. total is a
    # correctness knob: a session-verification evidence bundle is the
    # largest prompt this system sends and does not fit in 300s.
    timeouts:
      spawn_seconds: 10
      first_byte_seconds: 30
      total_seconds: 1200

# Model discovery. The direct-API path enumerates each vendor's models
# endpoint; the seat cannot enumerate — the CLI has no list-models command —
# so its catalog stays probe-confirmed. Enumeration is a metadata request and
# bills no tokens, which is why max_age_hours is a freshness preference and
# not a budget control. seat_max_age_hours is far longer because a probe does
# cost premium requests, and a warning that fires every day is a warning that
# gets suppressed. Neither threshold ever blocks anything.
#
# record resolves against the project root, not this file's directory, and
# lives under the gitignored .dabbler/ tree: it is derived from whichever key
# set is present, so it is neither committed nor packaged. The seat catalog
# ships because it belongs to the distribution; this one belongs to a machine.
discovery:
  key_set_id: default
  record: .dabbler/api-models.lock
  max_age_hours: 24
  seat_max_age_hours: 720

# --- Model Registry ---
# is_enabled: in the routed pool at all (identity-only entries set false —
#   they exist so orchestrator identity can resolve).
# is_enabled_as_verifier: trusted to review another model's output. It is a
#   property of the record rather than of a role, because it says what a model
#   may be asked to do and not which order the candidates are tried in.
models:
  gemini-flash:
    provider: google
    model_id: gemini-2.5-flash
    is_enabled: true
    is_enabled_as_verifier: true
    max_context_tokens: 1048576
    max_output_tokens: 65536
    system_prompt_file: prompt-templates/system-prompts.md
    notes: "Best for simple analysis, formatting, boilerplate generation."
    generation_params:
      thinking_budget: 0

  gemini-pro:
    provider: google
    model_id: gemini-2.5-pro
    is_enabled: true
    is_enabled_as_verifier: true
    max_context_tokens: 1048576
    max_output_tokens: 65536
    system_prompt_file: prompt-templates/system-prompts.md
    notes: "Medium-complexity analysis, documentation, code review."
    generation_params:
      thinking_budget: -1

  gemini-3-1-pro:
    provider: google
    model_id: gemini-3.1-pro-preview
    is_enabled: true
    # New model: generator + explicit-call only until calibrated.
    is_enabled_as_verifier: false
    max_context_tokens: 1048576
    max_output_tokens: 65536
    system_prompt_file: prompt-templates/system-prompts.md
    notes: "Frontier Google Pro (preview id; may change at GA)."
    generation_params:
      thinking_level: HIGH

  sonnet:
    provider: anthropic
    model_id: claude-sonnet-5
    is_enabled: true
    is_enabled_as_verifier: true
    max_context_tokens: 200000
    max_output_tokens: 16000
    system_prompt_file: prompt-templates/system-prompts.md
    notes: "Anthropic quality without Opus cost."
    generation_params:
      effort: medium
      thinking:
        enabled: false
        type: adaptive

  opus:
    provider: anthropic
    model_id: claude-opus-5
    is_enabled: true
    is_enabled_as_verifier: true
    max_context_tokens: 200000
    max_output_tokens: 32000
    system_prompt_file: prompt-templates/system-prompts.md
    notes: "Reserved for complex architectural reasoning."
    generation_params:
      effort: high
      thinking:
        enabled: true
        type: adaptive

  fable:
    provider: anthropic
    model_id: claude-fable-5
    # Registered, not routed: enabling it is an operator decision, not a
    # default.
    is_enabled: false
    is_enabled_as_verifier: false
    max_context_tokens: 200000
    max_output_tokens: 32000
    system_prompt_file: prompt-templates/system-prompts.md
    notes: "Claude Fable 5. Present so a Fable-5 orchestrator can record its identity."

  gpt-5-4:
    provider: openai
    model_id: gpt-5.4
    is_enabled: true
    is_enabled_as_verifier: true
    max_context_tokens: 272000
    max_output_tokens: 32000
    system_prompt_file: prompt-templates/system-prompts.md
    notes: "Frontier OpenAI model; cross-provider verifier for Anthropic/Google output."
    generation_params:
      reasoning_effort: high

  gpt-5-5:
    provider: openai
    model_id: gpt-5.5
    is_enabled: true
    is_enabled_as_verifier: true
    max_context_tokens: 272000
    max_output_tokens: 32000
    system_prompt_file: prompt-templates/system-prompts.md
    notes: "Frontier OpenAI model (GPT-5.5). Cross-provider verifier + consensus engine."
    generation_params:
      reasoning_effort: high

  gpt-5-6-sol:
    provider: openai
    model_id: gpt-5.6-sol
    is_enabled: true
    is_enabled_as_verifier: true
    max_context_tokens: 272000
    max_output_tokens: 32000
    system_prompt_file: prompt-templates/system-prompts.md
    notes: "GPT-5.6 'Sol' — the most capable of the 5.6 variants."
    generation_params:
      reasoning_effort: high

  gpt-5-6-luna:
    provider: openai
    model_id: gpt-5.6-luna
    is_enabled: true
    # Enabled so a discovery fan-out can prefer it by name, kept out of the
    # verifier pool so it can never review work it is not strong enough to
    # judge.
    is_enabled_as_verifier: false
    max_context_tokens: 272000
    max_output_tokens: 32000
    system_prompt_file: prompt-templates/system-prompts.md
    notes: "GPT-5.6 'Luna' — breadth-of-findings work only."
    generation_params:
      reasoning_effort: high

  haiku:
    provider: anthropic
    model_id: claude-haiku-4-5-20251001
    is_enabled: true
    # Enabled so a follow-through study can prefer it by name, kept out of the
    # verifier pool for the same reason as Luna.
    is_enabled_as_verifier: false
    max_context_tokens: 200000
    max_output_tokens: 8000
    system_prompt_file: prompt-templates/system-prompts.md
    notes: "Anthropic's small model. Used as a stand-in for a reader who will not puzzle things out."

  gpt-5-4-mini:
    provider: openai
    model_id: gpt-5.4-mini
    is_enabled: true
    is_enabled_as_verifier: true
    max_context_tokens: 400000
    max_output_tokens: 16000
    system_prompt_file: prompt-templates/system-prompts.md
    notes: "Small OpenAI model; cross-provider verifier for Gemini output."
    generation_params:
      reasoning_effort: medium

  # --- Identity-registry-only entries ---
  # Orchestrator models outside the routed pool that must still resolve in
  # the registry so identity resolution and the verifier exclusion can
  # derive their effective provider. is_enabled: false keeps them out of
  # every selection path.
  gemini-3-pro:
    provider: google
    model_id: gemini-3-pro-preview
    is_enabled: false
    is_enabled_as_verifier: false
    notes: "Identity registry only: Gemini Code Assist orchestrator sessions."

task_type_params:
  formatting:
    sonnet: {effort: low, thinking: {enabled: false}}
    opus: {effort: low, thinking: {enabled: false}}
    gemini-flash: {thinking_budget: 0}
    gemini-pro: {thinking_budget: 128}   # Pro rejects 0 ("only works in thinking mode"); 128 is its minimum
    gpt-5-4-mini: {reasoning_effort: none}
    gpt-5-4: {reasoning_effort: none}
  summarization:
    sonnet: {effort: low, thinking: {enabled: false}}
    opus: {effort: low, thinking: {enabled: false}}
    gemini-flash: {thinking_budget: 0}
    gemini-pro: {thinking_budget: 128}   # Pro rejects 0 ("only works in thinking mode"); 128 is its minimum
    gpt-5-4-mini: {reasoning_effort: none}
    gpt-5-4: {reasoning_effort: low}
  documentation:
    sonnet: {effort: low, thinking: {enabled: false}}
    gemini-pro: {thinking_budget: -1}
    gpt-5-4-mini: {reasoning_effort: low}
  session-close-out:
    sonnet: {effort: low, thinking: {enabled: false}}
    gemini-pro: {thinking_budget: 128}   # Pro rejects 0 ("only works in thinking mode"); 128 is its minimum
    gpt-5-4-mini: {reasoning_effort: none}
  test-generation:
    sonnet: {effort: medium, thinking: {enabled: true, type: adaptive}}
    gemini-pro: {thinking_budget: -1}
    gpt-5-4-mini: {reasoning_effort: medium}
  code-review:
    sonnet: {effort: medium, thinking: {enabled: true, type: adaptive}}
    gemini-pro: {thinking_budget: -1}
    gpt-5-4-mini: {reasoning_effort: medium}
  analysis:
    sonnet: {effort: medium, thinking: {enabled: true, type: adaptive}}
    gemini-pro: {thinking_budget: -1}
    gpt-5-4-mini: {reasoning_effort: medium}
  refactoring:
    sonnet: {effort: medium, thinking: {enabled: true, type: adaptive}}
    gemini-pro: {thinking_budget: -1}
    gpt-5-4-mini: {reasoning_effort: medium}
  session-verification:
    sonnet: {effort: high, thinking: {enabled: true, type: adaptive}}
    opus: {effort: high, thinking: {enabled: true, type: adaptive}}
    gemini-pro: {thinking_budget: -1}
    gpt-5-4: {reasoning_effort: high}
    gpt-5-4-mini: {reasoning_effort: high}
  security-review:
    sonnet: {effort: high, thinking: {enabled: true, type: adaptive}}
    opus: {effort: high, thinking: {enabled: true, type: adaptive}}
    gemini-pro: {thinking_budget: -1}
    gpt-5-4: {reasoning_effort: high}
  architecture:
    opus: {effort: max, thinking: {enabled: true, type: adaptive}}
    gemini-pro: {thinking_budget: -1}
    gpt-5-4: {reasoning_effort: xhigh}
  planning:
    opus: {effort: high, thinking: {enabled: true, type: adaptive}}
    gemini-pro: {thinking_budget: -1}
    gpt-5-4: {reasoning_effort: high}
  verification:
    sonnet: {effort: medium, thinking: {enabled: true, type: adaptive}}
    opus: {effort: high, thinking: {enabled: true, type: adaptive}}
    gemini-pro: {thinking_budget: -1}
    gpt-5-4: {reasoning_effort: high}
    gpt-5-4-mini: {reasoning_effort: medium}

escalation:
  enabled: true
  # A retry walks down the role's ordered candidate list; this is how far.
  max_escalations: 2
  triggers:
    empty_response: true
    max_tokens_hit: true
    min_output_tokens: 30
    refusal_detection: true
  refusal_phrases:
    - "i can't help with"
    - "i'm unable to"
    - "i cannot assist"
    - "beyond my capabilities"
    - "i don't have enough context"

# Cross-provider verification. Session 2 wires the loop; the settings here
# are read by the verifier selection and prompt template resolution.
verification:
  enabled: true
  auto_verify_task_types:
    - code-review
    - security-review
    - architecture
    - documentation
  settings:
    # The session-verification loop suspends at this bound. The closed
    # severity vocabulary is the primary control; the cap is the backstop.
    max_rounds: 3
    # The tests phase runs the authored tests at most this many times before
    # its loop terminates. Separate from max_rounds because a test round
    # runs a suite the framework already has, where a review round buys a
    # vendor's opinion.
    max_test_rounds: 7
    on_disagreement: merge     # merge | flag | re-route
    on_disagreement_by_task_type:
      code-review: merge
      security-review: merge
      test-generation: merge
      documentation: merge
      analysis: flag
      planning: flag
      architecture: flag
      refactoring: flag
    tiebreaker_model: opus
    prompt_template_file: prompt-templates/verification.md

metrics:
  enabled: true
  log_filename: router-metrics.jsonl

# Step (f) of the session lifecycle: pack, then push to a feed. A session that
# declared itself releasable at step (a) publishes through `packaging.pack`
# and `packaging.push` — both argv, never shell strings, both spawned with the
# child-environment allowlist, and `push` naming the credential rather than
# holding it. The framework substitutes `{output}`, `{artifact}`, `{feed}` and
# `{secret}` per argv element at spawn time.
#
# This repository declares none, and that is the declaration: it publishes to
# no feed today. A block here would have to carry a feed URL nobody publishes
# to, which is the shape of the mistake that put a machine-local record inside
# package data — a plausible-looking default that a build would have shipped.
# The real one belongs in `local-overrides.yaml`, which is gitignored and is
# where a machine states machine facts:
#
#   packaging:
#     pack:
#       argv: ["dotnet", "pack", "-c", "Release", "-o", "{output}"]
#     push:
#       argv: ["dotnet", "nuget", "push", "{artifact}",
#              "--source", "{feed}", "--api-key", "{secret}"]
#       feed: https://pkgs.dev.azure.com/<org>/_packaging/<feed>/nuget/v3/index.json
#       secret: DABBLER_FEED_PAT

# Which tests a change makes necessary, and what proves the suite was green.
#
# Pre-verification runs the selected tests only. The full suite is recorded
# once, against the final verified tree -- never as ordinary pre-verification
# evidence.
testing:
  suites:
    - name: python
      command: python -m pytest
      expensive: true
      covers:
        - ai_router/
        - tests/
        - pyproject.toml
        - pytest.ini

  # Deterministic controls are declared here — one entry per kind, from
  # compile, typecheck, lint, analyzer. A declared control runs before
  # verification and its exit code is the fact; `required` sends a red
  # result back to the author instead of buying a verifier's opinion on
  # something the exit code already settled. This repository declares none:
  # it ships no typechecker, linter, or analyzer, and the record says
  # `not_applicable` rather than inventing a green row for a tool nobody
  # runs.

  selection:
    # Where the tests live and what this repository calls them. Declared,
    # because a framework that guesses either one has guessed an ecosystem.
    test_roots:
      - tests
    test_glob: "test_*.py"

    # Changing one of these changes what every test does, so the selector can
    # prove the whole suite is affected instead of an operator asserting it.
    repo_wide:
      - tests/conftest.py
      - pytest.ini
      - pyproject.toml

    # Run when a changed path maps to nothing. Cheap, broad, and enough to
    # notice a structural break -- not a substitute for a real mapping.
    smoke:
      - tests/test_config.py

    # Which tests answer for which path. Every mapping is declared here:
    # nothing is inferred from imports, because inferring it costs a parser
    # per language and buys an economy, not a proof. The proof is the
    # complete suite against the final verified tree, so a rule that has gone
    # stale costs a late discovery there and cannot ship a defect.
    #
    # A rule names the tests that would notice this path breaking -- the ones
    # that drive it, directly or through one thin caller. An empty `select`
    # declares "this path affects no test", which is a mapping, not an
    # unknown. A path no rule covers records `selection_unknown`, runs the
    # smoke tests, and raises a risk; that is the loud state, and it is the
    # signal that a rule is missing.
    rules:
      - when: ai_router/__init__.py
        select:
          - tests/test_route.py
      - when: ai_router/affected.py
        select:
          - tests/test_affected.py
          - tests/test_verify.py
          - tests/test_step_execution.py
          - tests/test_agency.py
      - when: ai_router/approved_plan.py
        select:
          - tests/test_approved_plan.py
          - tests/test_session.py
          - tests/test_step_execution.py
      - when: ai_router/bootstrap.py
        select:
          - tests/test_bootstrap.py
          - tests/test_step_execution.py
      - when: ai_router/checks.py
        select:
          - tests/test_runcore_checks.py
          - tests/test_runcore_verified.py
          - tests/test_runcore_fast.py
          - tests/test_testphase.py
          - tests/test_fixloop.py
      - when: ai_router/config.py
        select:
          - tests/test_config.py
          - tests/test_critique_contracts.py
          - tests/test_route.py
          - tests/test_verify.py
          - tests/test_workflow.py
      - when: ai_router/discovery.py
        select:
          - tests/test_discovery.py
          - tests/test_session.py
      - when: ai_router/evidence.py
        select:
          - tests/test_evidence_protocol.py
          - tests/test_affected.py
          - tests/test_critique_contracts.py
          - tests/test_gates.py
          - tests/test_session.py
          - tests/test_verify.py
      - when: ai_router/facts.py
        select:
          - tests/test_facts.py
          - tests/test_verify.py
          - tests/test_step_execution.py
      - when: ai_router/fixloop.py
        select:
          - tests/test_fixloop.py
          - tests/test_workflow.py
      - when: ai_router/gates.py
        select:
          - tests/test_gates.py
          - tests/test_session.py
          - tests/test_verify.py
      - when: ai_router/identity.py
        select:
          - tests/test_identity.py
          - tests/test_session.py
          - tests/test_verify.py
      - when: ai_router/journal.py
        select:
          - tests/test_journal.py
          - tests/test_runcore_contracts.py
          - tests/test_runcore_recovery.py
          - tests/test_runcore_fast.py
      - when: ai_router/ledger.py
        select:
          - tests/test_ledger.py
          - tests/test_affected.py
          - tests/test_critique_contracts.py
          - tests/test_gates.py
          - tests/test_session.py
          - tests/test_packaging.py
          - tests/test_verify.py
          - tests/test_step_execution.py
      - when: ai_router/lockfile.py
        # Both discovery records render and stamp through this module, so a
        # change here can break either one.
        select:
          - tests/test_discovery.py
          - tests/test_transport_copilot.py
      - when: ai_router/metrics.py
        select:
          - tests/test_metrics.py
          - tests/test_route.py
          - tests/test_verify.py
      - when: ai_router/modules.py
        select:
          - tests/test_modules.py
      - when: ai_router/packaging.py
        select:
          - tests/test_packaging.py
      - when: ai_router/plan_review.py
        select:
          - tests/test_plan_review.py
          - tests/test_step_execution.py
      - when: ai_router/pricing.py
        select:
          - tests/test_pricing.py
          - tests/test_selection.py
          - tests/test_metrics.py
          - tests/test_route.py
      - when: ai_router/progress.py
        select:
          - tests/test_progress.py
          - tests/test_session.py
          - tests/test_gates.py
          - tests/test_verify.py
      - when: ai_router/route.py
        select:
          - tests/test_route.py
          - tests/test_escalation.py
          - tests/test_verify.py
      - when: ai_router/runcli.py
        select:
          - tests/test_runcore_fast.py
          - tests/test_runcore_verified.py
          - tests/test_runcore_checks.py
          - tests/test_runcore_recovery.py
          - tests/test_runcore_contracts.py
          - tests/test_runcore_independence.py
      - when: ai_router/runcore.py
        select:
          - tests/test_runcore_contracts.py
          - tests/test_runcore_fast.py
          - tests/test_runcore_verified.py
          - tests/test_runcore_recovery.py
          - tests/test_runcore_independence.py
      - when: ai_router/runproject.py
        select:
          - tests/test_runcore_fast.py
          - tests/test_runcore_contracts.py
          - tests/test_runcore_recovery.py
          - tests/test_runcore_independence.py
          - tests/test_runcore_verified.py
      - when: ai_router/runtime_mode.py
        select:
          - tests/test_route.py
          - tests/test_verify.py
      - when: ai_router/seat_cost.py
        select:
          - tests/test_seat_cost.py
      - when: ai_router/secret_resolver.py
        select:
          - tests/test_selection.py
          - tests/test_route.py
          - tests/test_transport_api.py
      - when: ai_router/selection.py
        select:
          - tests/test_selection.py
          - tests/test_route.py
          - tests/test_escalation.py
      - when: ai_router/session.py
        select:
          - tests/test_session.py
          - tests/test_affected.py
          - tests/test_bootstrap.py
          - tests/test_critique_contracts.py
          - tests/test_gates.py
          - tests/test_verify.py
      - when: ai_router/stepreview.py
        select:
          - tests/test_stepreview.py
          - tests/test_workflow.py
      - when: ai_router/test_evidence.py
        select:
          - tests/test_evidence_protocol.py
          - tests/test_affected.py
          - tests/test_gates.py
          - tests/test_verify.py
      - when: ai_router/testphase.py
        select:
          - tests/test_testphase.py
          - tests/test_workflow.py
          - tests/test_fixloop.py
      - when: ai_router/verdict.py
        select:
          - tests/test_verdict.py
          - tests/test_gates.py
          - tests/test_session.py
          - tests/test_verify.py
          - tests/test_workflow.py
      - when: ai_router/verify.py
        select:
          - tests/test_verify.py
          - tests/test_critique_contracts.py
          - tests/test_step_execution.py
          - tests/test_agency.py
      - when: ai_router/agency.py
        select:
          - tests/test_agency.py
          - tests/test_verify.py
          - tests/test_testphase.py
          - tests/test_fixloop.py
      - when: ai_router/verifyjob.py
        select:
          - tests/test_runcore_verified.py
          - tests/test_runcore_contracts.py
          - tests/test_verify.py
      - when: ai_router/workflow.py
        select:
          - tests/test_workflow.py
      - when: ai_router/writers.py
        select:
          - tests/test_session.py
          - tests/test_progress.py
          - tests/test_bootstrap.py
          - tests/test_gates.py
      - when: ai_router/transports/__init__.py
        select:
          - tests/test_transport_api.py
          - tests/test_escalation.py
      - when: ai_router/transports/api.py
        select:
          - tests/test_transport_api.py
          - tests/test_route.py
      - when: ai_router/transports/base.py
        select:
          - tests/test_escalation.py
          - tests/test_transport_api.py
          - tests/test_transport_copilot.py
          - tests/test_route.py
      - when: ai_router/transports/copilot.py
        select:
          - tests/test_transport_copilot.py
          - tests/test_bootstrap.py
          - tests/test_identity.py
          - tests/test_route.py
          - tests/test_agency.py
      - when: ai_router/router-config.yaml
        select:
          - tests/test_config.py
          - tests/test_route.py
          - tests/test_packaging.py
      - when: ai_router/schemas/
        select:
          - tests/test_critique_contracts.py
          - tests/test_approved_plan.py
          - tests/test_plan_review.py
          - tests/test_step_execution.py
          - tests/test_ledger.py
          - tests/test_agency.py
          - tests/test_verify.py
          - tests/test_gates.py
          - tests/test_runcore_contracts.py
          - tests/test_packaging.py
      - when: ai_router/prompt-templates/
        select:
          - tests/test_verify.py
      - when: ai_router/copilot-catalog.lock
        select:
          - tests/test_transport_copilot.py
      - when: tests/fixtures/
        select:
          - tests/test_modules.py
      - when: docs/
        select: []
      - when: .github/
        select: []
      - when: README.md
        select: []
      - when: STATUS.md
        select: []
      - when: AGENTS.md
        select: []
      - when: CLAUDE.md
        select: []
      - when: GEMINI.md
        select: []
      - when: .gitignore
        select: []
