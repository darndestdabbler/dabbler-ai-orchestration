# telemetry / dabbler-platform — LEAD signal

Awaiting the first pilot run. `dabbler-platform` is the **lead** finding-power signal
(C# generator tooling, source-dominated) — pilot set `access-migration-generator-consumption`.

Each build session drops a `<session>/` subdirectory here with its frozen
`matrix-run/` artifacts + `metadata.json` (`includedInFindingPower=true`,
`diffClass=source-dominated`). Contract: [`../README.md`](../README.md).
