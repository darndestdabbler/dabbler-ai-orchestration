VERIFIED

I tried to break the recommendation by looking for a fourth likely change that
would cut across C the way it cuts across B. The closest I found was "read from
a stream instead of a path", which lands in `csv-app` and `csv-parser` — two of
three, no worse than C's own table admits. The Parnas citation is used
correctly rather than decoratively: candidate B is rejected for the reason the
paper actually gives.

NITS

- **Severity:** Minor. Candidate A is dismissed slightly too quickly. For a
  solution this size it is a defensible choice, and saying so would make the
  recommendation stronger rather than weaker.
