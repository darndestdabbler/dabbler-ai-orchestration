**VERIFIED** — I reviewed the fix-delta code paths around lifecycle-written files, including the shared constant in `ai_router/ledger.py`, its imports in envelope comparison, facts, gates, and test evidence, plus the related tests and replay report. The change preserves the previous behavior while centralizing the lifecycle bookkeeping list, and I found no blocking correctness or completeness defects in the delta.