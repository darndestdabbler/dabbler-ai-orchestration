ISSUES FOUND

- **Issue 1:** `close_preflight` omits the set-terminal path-aware critique and contract gates.
  - **Category:** Completeness
  - **Severity:** Major
  - **Evidence paths:** `ai_router/close_preflight.py:430`, `ai_router/close_preflight.py:511`, `ai_router/close_session.py:2059`, `ai_router/close_session.py:2164`, `ai_router/tests/test_path_aware_critique_close_gate.py:193`, `ai_router/tests/test_contract_gate_close.py:189`
  - **Failure scenario:** A terminal session in a set with `pathAwareCritique: required` or `contractGate: required` runs preflight before close. The report never mentions the missing artifact/floor obligation because it only walks `GATE_CHECKS` plus `verification_backstop`; then `close_session` reaches the terminal gate and hard-blocks in an interactive TTY. This is probable because these are first-class close gates with dedicated tests and many session sets opt into path-aware critique.
  - **Acceptance criterion:** `JUDGMENT - close_preflight reports path_aware_critique_gate and contract_gate when the same set-terminal conditions would make close_session evaluate them, preserving required/advisory/non-TTY blocking posture and remediation.`
  - **Details:** **Violation:** the task requires the preflight to “evaluate the same predicates the close evaluates” and print “every unmet obligation in one pass”; the implementation only iterates `GATE_CHECKS` and appends `verification_backstop`, while `close_session` evaluates two additional terminal close gates afterward. **Impact:** the tool can say it named everything standing between work and closed while silently omitting obligations that can block the actual close, defeating the central completeness promise. **Evidence:** `close_session` has path-aware critique and contract gate failure branches that emit `closeout_failed`; their tests prove required missing artifacts hard-block in TTY, but `close_preflight.preflight_check_names()` and `evaluate()` contain no corresponding checks.