**VERIFIED** — I checked the current `workflow.py` delta against the prior failure modes: same-step `enter` now no longer clears or resets loop state in `fold()`, `review_cap(root)` still resolves config through the supplied workspace root, and `status` now renders the review-loop label unconditionally for the solution and each component. The matching regression coverage in `tests/test_workflow.py` exercises the same-step preservation path and the non-CWD cap lookup, and I did not find a remaining blocking defect in this fix delta.