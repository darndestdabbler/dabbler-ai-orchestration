.venv/
__pycache__/
*.pyc

# The `Try a sample project` resume marker. Written while the sample is being
# created and removed once it succeeds -- so if it were tracked, its removal
# would leave a dirty working tree and the first close_session would fail its
# working_tree_clean gate. `.dabbler/local-only` is deliberately NOT ignored:
# that one is the durable record the close-out push gate reads.
.dabbler/sample-in-progress.json
