**VERIFIED** — I checked the close-mandated write discovery/normalization, freshness digest behavior, backstop baseline/remediation path, oversized-evidence exception handling, and deleted public modules/exports. I found no Critical or Major defects.

**NITS**
- **Nit:** The no-baseline `--phase remediation-review` refusal text still mentions only `sN-issues*.json` envelopes even though the reader now also checks `sN-rounds.jsonl`; that is recoverable wording, not a blocking behavior issue.