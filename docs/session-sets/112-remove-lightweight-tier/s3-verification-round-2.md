ISSUES FOUND

- **Issue 1:** The anti-resurrection gate skips live `.template` scaffold sources.
  - **Category:** Correctness
  - **Severity:** Major
  - **Failure scenario:** A future normal scaffold edit reintroduces `tier: lightweight` or `verificationMode:` into `docs/templates/consumer-bootstrap/spec.md.template`. CI still passes because `.template` is not scanned, then the extension ships a template that generates specs the 1.0 router refuses. This is probable because these templates are the canonical live source for new consumer specs and were part of the tier-removal surface.
  - **Acceptance criterion:** JUDGMENT - The guard scans live `.template` scaffold files, and a planted `tier: lightweight` or `verificationMode:` declaration in `docs/templates/consumer-bootstrap/spec.md.template` makes the guard fail while legitimate template narration remains allowed.
  - **Details:** Violation: the task required “a script/test asserting zero live references to `tier: lightweight`, `verificationMode`, or either mode outside archives,” and the gate claims fenced/YAML declaration territory is scanned. Impact: the executable proof does not cover the repo’s live scaffold source, so a broken consumer-facing spec can resurrect silently. Evidence: `ai_router/scripts/lightweight_resurrection_guard.py:337-350` omits `.template` from `_BLANKERS`, and `:373-376` skips any suffix not listed. `docs/templates/consumer-bootstrap/spec.md.template` is the canonical spec scaffold, wired by `tools/dabbler-ai-orchestration/src/utils/consumerBootstrap.ts:120-123` and rendered by `:355-360`; its own README says reintroducing these fields would hand every new consumer repo a spec that cannot run. I also probed a temp repo containing only `spec.md.template` with `tier: lightweight`, and the guard exited 0 with `[ok]`.