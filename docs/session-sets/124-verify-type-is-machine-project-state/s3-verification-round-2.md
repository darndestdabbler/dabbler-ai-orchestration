**VERIFIED** — I checked the live Python writer/CLI, the extension seat-setup subprocess path and VS Code wrapper, the updated tests, and the named docs/templates/cold-start fixture. I found no new Critical/Major defect beyond the already-reported stderr-warning issue; the retired `local-overrides.yaml` write is gone, the new write routes through `verify_type --set ... --project-root`, and the cold-start walk is recorded in the session activity log.