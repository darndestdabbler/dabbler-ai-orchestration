**VERIFIED** — I checked the actual remediation hunks and adjacent catalog serialization path. The allowlist is now path-scoped, the doc/comment changes are consistent with the current `ModelEntry`/`discover_catalog()` contracts, and I found no Critical/Major defect introduced by this delta.

No non-exempt ledger ids were present, so there are no fix-verdict lines to emit.