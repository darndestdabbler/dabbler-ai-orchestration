{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "approved-plan.json (the pre-registered, hashed step-by-step plan for one session)",
  "description": "The machine-owned plan for one session's own work, under .dabbler/runs/<set>/s<N>/approved-plan.json. Written only through ai_router.approved_plan: validated against this schema, then atomic replace. Before approval the plan may be rewritten in full; after approval (approved: true) it is immutable except by an appended amendment -- plan_hash is computed over every field except 'amendments', so an appended amendment never changes it and any other edit is detected the next time the plan is read. The plan holds only the session's own steps, never the fixed lifecycle ceremony (register, affected tests, verification, run of record, close-out) that surrounds every session.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version", "session_set", "session_number", "session_slug",
    "steps", "approved", "amendments"
  ],
  "properties": {
    "schema_version": {
      "const": 1,
      "description": "Frozen at v1. A reader that finds any other value refuses the record rather than interpreting it."
    },
    "session_set": { "type": "string", "minLength": 1 },
    "session_number": { "type": "integer", "minimum": 1 },
    "session_slug": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$",
      "description": "The session's authored slug -- the same short, hand-picked label a session set already carries in its own directory name, one level down."
    },
    "steps": {
      "type": "array",
      "minItems": 1,
      "maxItems": 7,
      "description": "At most seven -- refused here, not counted by a reviewer. The lifecycle ceremony around every session (register, affected tests, verification, run of record, close-out) is not a step and never enters this array.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "step_id", "intent", "file_envelope", "evidence_contract",
          "risk_flags"
        ],
        "properties": {
          "step_id": {
            "type": "string",
            "pattern": "^[a-z0-9-]+$",
            "description": "The one identity addressing this step in spec.md, activity-log.json and here."
          },
          "intent": {
            "type": "string",
            "minLength": 1,
            "description": "One sentence, imperative."
          },
          "file_envelope": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 },
            "description": "The paths this step may create or modify. Nothing else."
          },
          "evidence_contract": {
            "type": "array",
            "minItems": 1,
            "description": "What will prove this step was done correctly. Empty is invalid -- a step that declares no proof is refused here, not caught later by a reviewer.",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["description", "kind"],
              "properties": {
                "description": { "type": "string", "minLength": 1 },
                "kind": {
                  "type": "string",
                  "enum": ["deterministic", "judgment"],
                  "description": "'deterministic': a test, compile, lint, or analyzer result the framework can execute. 'judgment': needs a model to read something."
                }
              }
            }
          },
          "risk_flags": {
            "type": "array",
            "description": "Derived mechanically from the file envelope and the repository manifest -- never declared by the step's own author.",
            "items": {
              "type": "string",
              "enum": [
                "public-interface", "integration-module", "sensitive-path",
                "dependency-change"
              ]
            }
          }
        }
      }
    },
    "approved": { "type": "boolean" },
    "approved_at": { "type": ["string", "null"] },
    "plan_hash": {
      "type": ["string", "null"],
      "description": "sha256:<hex> over every field except 'amendments', bound in at approval."
    },
    "amendments": {
      "type": "array",
      "description": "Append-only. The only legal change to an approved plan. An amendment carries the change itself, not a note about it: 'added_files' widens the amended step's envelope and 'evidence_contract' replaces its proof, and the plan a reader acts on is the core folded with its amendments in order (approved_plan.effective_plan). The core is never rewritten, so plan_hash never moves.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["recorded_at", "step_id", "reason"],
        "properties": {
          "recorded_at": { "type": "string", "minLength": 1 },
          "step_id": { "type": "string", "minLength": 1 },
          "reason": { "type": "string", "minLength": 1 },
          "changed_fields": {
            "type": "array",
            "items": { "type": "string", "minLength": 1 }
          },
          "added_files": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 },
            "description": "Paths this amendment adds to the amended step's file envelope."
          },
          "evidence_contract": {
            "type": "array",
            "minItems": 1,
            "description": "The amended step's replacement proof. Present only when this amendment changes an evidence criterion.",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["description", "kind"],
              "properties": {
                "description": { "type": "string", "minLength": 1 },
                "kind": {
                  "type": "string",
                  "enum": ["deterministic", "judgment"]
                }
              }
            }
          }
        }
      }
    }
  },
  "allOf": [
    {
      "if": { "properties": { "approved": { "const": true } } },
      "then": { "required": ["approved_at", "plan_hash"] }
    }
  ]
}
