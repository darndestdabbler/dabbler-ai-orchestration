# Consult round 18 — gpt-5.6-sol via the Copilot seat (44 s; served gpt-5.6-sol)

1. **Recommendation:** Target “no unplanned human interruption,” with only two additional mid-session exceptions: a critical accountable decision and something only the person possesses or can authorize. · **Design:** Ask at planning for foreseeable credentials, remote access, spending/release authority, and product intent; interrupt later only for an unforeseeable security/legal/data-loss risk or missing human-only authority—the brief already identifies credentials, remotes, and spending decisions as human-owned. · **The one risk:** The framework may classify ordinary uncertainty as “critical” and repeatedly interrupt the developer. · **Mitigation:** Require every escalation to state the irreversible consequence and why author instruction, retry, rewind, hold-release, or safe cancellation cannot resolve it. · **Delete:** Do not build a general “ask the human” escape hatch.

2. **Recommendation:** Remove the generic Resume button, but replace it with automatic lifecycle recovery and a specific **Reconnect Authoring AI** action. · **Design:** Keep the shipped crash layer for a dead loop—two restarts per progress point, then a visible `crash` record; on VS Code activation, detect an in-flight persisted session and restart its loop; when the AI CLI dies, show “Authoring AI disconnected” with the exact engine-specific relaunch/reconnect command rather than “Resume.” This preserves round 17’s human recovery for a dead CLI without pretending one verb repairs three different failures. · **The one risk:** Automatic activation could revive a session the developer intentionally abandoned, resembling the reported hidden revival after closing the loop terminal. · **Mitigation:** Persist an explicit session-running intent, clear it on Stop/Cancel, and never detach-revive merely because a terminal was closed. · **Delete:** Delete Resume and any hidden detached restart not tied to persisted running intent.

3. **Recommendation:** Do not build an AI governor yet; the two beta failures expose deterministic lifecycle defects, not evidence that a person-facing impasse required an AI decision. · **Design:** Retain bounded deterministic retry/rewind and author instructions now; the round 17 revisit threshold was ten real sessions, while these are the first two, and both failures align with known defects such as contradictory release authority, invalid cancellation guidance, missing status rereads, lease theft, or silent waits. If later evidence meets the threshold, use the orchestrator’s closed-menu design with the Auxiliary Reviewer: it already adjudicates disputes, while the framework continues executing and recording every decision. · **The one risk:** A governor introduced now would mask defects, grant an exception around bad state, or mutate work for the wrong reason—the same failure mode round 17 identified for an AI mechanic. · **Mitigation:** Record deterministic recoveries and unresolved impasses until ten sessions, then build only for repeated cases that reached a person but required no human judgment. · **Delete:** Do not build the operator’s replacement driver; any future governor must never run git, write code or the ledger, execute gates, waive evidence, invent an action outside the closed menu, or judge its own authored work.

4. **Recommendation:** Add one persisted wait contract—owner, activity, start time, deadline, and last progress—and make both the extension and loop enforce it. · **Design:** Every live session must visibly say, for example, “Author owes step 4, waiting 2:13,” “Verification round running, 3:42 of 10:00,” or “Human action required”; framework jobs get hard deadlines, author waits get overdue notices rather than forced failure, and missing/expired progress enters bounded recovery before producing a visible Stop. This closes the brief’s cases where a live heartbeat can conceal a hung job or cancelled session and where a Stop may raise no notification. · **The one risk:** A heartbeat or elapsed timer presented as progress could still make a genuinely stuck operation look healthy. · **Mitigation:** Progress means a changed persisted milestone or job signal, not merely another heartbeat; always display elapsed time, deadline, last real progress, and current owner. · **Delete:** Delete indefinite polling, bare “working” indicators, and minute-by-minute waiter re-arming with no explanation.

5. **Recommendation:** Fix the four verified lifecycle invariants—1, 2, 3, and 5—in cohesive work, then add the wait net; do not create seven isolated remediation sessions or ask a governor to absorb known bugs. · **Design:** Establish one release decision sourced from the checkout setting and plan; print only commands that parse and prevent every supported engine from forcing cancellation or close; make the loop reread terminal status before every transition and before commit/push; and prohibit `session next` from stealing a live mailbox lease. The wait contract covers candidates 7 and 8, while reported items 6, 7, 8, 9, and 10 should first be verified and then fixed where confirmed—especially hidden revival and commit/push after terminal closure. · **The one risk:** Because beta failure two has four candidate causes, fixing only the most plausible one could leave the developer seeing the same hang. · **Mitigation:** Treat “no silent wait” as the acceptance invariant, so an unfixed cause must still become a named, timed, visible failure. · **Delete:** Delete invalid ways-on, AI-facing force-cancel guidance, and all framework text directing an AI to `session next` while a mailbox loop owns the lease.

6. **Recommendation:** Build two sessions: lifecycle authority first, then bounded and visible waiting; build no governor. · **Design:** Session one unifies release classification, removes engine cancellation/close authority, makes terminal status observable by the loop, and protects the mailbox lease; session two adds the persisted wait contract, job deadlines, Stop notification, and activation recovery. Together these directly address beta one’s false releasability/self-cancellation/uncommitted ending and every listed candidate for beta two’s unexplained wait. · **The one risk:** Happy-path tests may pass while races between cancellation, lease saves, commit, and process death remain. · **Mitigation:** The proof experiment is failure injection: replay declaration-before-plan on a publish-nothing consumer; attempt force cancellation from Claude, Copilot, and Codex; cancel between phase transitions and immediately before commit; steal the lease; hang a verification job; suppress Stop notification; and kill the loop/editor/AI CLI independently—each run must end in correct release status, no unauthorized commit or push, one durable instruction, and a visible named owner or bounded failure. · **Delete:** Do not use broad end-to-end success as the proof; test the exact failure interleavings.

7. **Recommendation:** Delete mechanisms that duplicate authority or turn recoverable conditions into ambiguous process control. · **Design:** Remove the generic Resume button, dual release declarations, invalid Stop commands, AI-accessible `--force`, `session next` guidance under mailbox ownership, indefinite job polling, hidden detached revival, and process-ending Stops for conditions deterministic retry, rewind, or author instruction can resolve; retain durable Stops only for genuine external or human-required boundaries. · **The one risk:** Removing manual escape hatches before automatic recovery is proven could leave developers more trapped than today. · **Mitigation:** Sequence deletion behind the replacement’s failure-injection proof and retain one explicit **Stop Session** action plus engine-specific reconnect. · **Delete:** Do not build an AI mechanic, an AI replacement driver, a state-machine-heavy recovery architecture, or a new reviewer role; also remove the Auxiliary Reviewer’s test-root write power if it is ever used as governor, because a role granting exceptions must not alter the evidence it governs.
