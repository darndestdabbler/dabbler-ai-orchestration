**VERIFIED** — The prior `--machine-scope` finding is resolved: `persist_transport_preference` now skips machine writes when unelevated, falls back to user scope when machine persistence fails, returns the landed scope, and `main()` reports downgraded user-scope persistence. Routed Copilot dispatches are centralized through `_build_argv`, which now includes `--no-custom-instructions` for both inline and handoff paths, with tests covering both.