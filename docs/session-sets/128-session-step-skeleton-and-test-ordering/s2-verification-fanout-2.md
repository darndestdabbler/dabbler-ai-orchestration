ISSUES FOUND

- **Issue 1:** A4.2 is only reported; the close backstop still runs an unphased full verification round for shipped-code post-round deltas.
  - **Category:** Correctness
  - **Severity:** Major
  - **Evidence paths:** `ai_router/close_backstop.py:803`, `ai_router/close_backstop.py:887`, `ai_router/close_backstop.py:918`, `ai_router/close_backstop.py:1149`, `docs/planning/session-set-authoring-guide.md:877`, `docs/planning/session-set-authoring-guide.md:907`, `docs/session-sets/128-session-step-skeleton-and-test-ordering/decisions.jsonl:4`
  - **Failure scenario:** A session verifies, runs the full suite, fixes a shipped-code failure, and proceeds to close without manually running `verify_session --phase remediation-review`. That is the named A4.2 path. The stale stamped row will not settle, `decide_backstop` will route, and `run_close_backstop` deterministically calls `_vs.assemble_evidence(...)` with no remediation-review framing and records `phase=None`, so the user buys the classic open backstop round every time instead of the promised delta-scoped review.
  - **Acceptance criterion:** `JUDGMENT - A shipped-code post-round delta must no longer take the classic unphased backstop path; close/backstop must either execute or require a `remediation-review` phase round, and product tests must cover that path.`
  - **Details:** **Violation:** the documented/operator-ratified rule says “A4.2 — a post-suite fix to code triggers targeted/focused remediation-review only, not an open re-verification,” and the authoring guide claims “the close backstop consults the same classifier rather than buying an unphased full round.” **Impact:** the main A4.2 objective is not delivered: the close path still spends the expensive broad verification round and allows out-of-delta review scope, changing the merge decision because the mechanized half of A4.2 is incomplete. **Evidence:** `_a4_obligation_note` explicitly “Reports; never decides,” `run_close_backstop` only appends that note after routing, then assembles classic evidence via `_vs.assemble_evidence(...)` and records the completed round with `phase=None`. The correct behavior is to avoid the unphased backstop for `DELTA_SHIPPED_CODE` and make the owed round a remediation-review.