VERIFIED — I tried to break the anchored ignore rule, `verify_type.py`’s retired `committed` API/vocabulary, and the test-suite guard across resolver/derivation seams. The live tree has the root verify-type file ignored and untracked, the CLI resolves branch 1 to `COPILOT_CLI`, no in-scope `.committed` consumer remains, and the targeted Python coverage for these paths passes; I found no Critical/Major issues or material nits.