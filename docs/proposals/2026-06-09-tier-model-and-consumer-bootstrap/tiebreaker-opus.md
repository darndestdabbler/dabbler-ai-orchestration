{"pick":"A","confidence":"medium","rationale":"The decisive criterion is a COLD orchestrator (possibly a different engine, no memory) self-orienting with zero 'what next?' ambiguity. The uniform lifecycle (ensure .venv+package, locate active spec.md, read tier+verificationMode, run start_session routed-or-no-router, close via the shared gate) is identical across all sets and both tiers, so it must NOT live in per-set spec.md (that would duplicate procedure into every set = real drift). spec.md is per-set CONTENT, not the procedure. Option B therefore forces the operative steps to live either in the 'thin' engine files (breaking the settled thinness) or behind the canonical URL (network-dependent cold start). Option A puts the invariant procedure in ONE generated, never-hand-edited local operative file, giving a deterministic, offline, engine-agnostic runbook — exactly the zero-ambiguity, max-uniformity target. The extra file is generated from the same template bundle with snapshot tests, so it adds build-time drift surface, not hand-edit surface, which is consistent with the operator's 'no hand-editing except templated spec.md' constraint.","refinement":"start-here.md must resolve THE active spec.md deterministically with a single, machine-checkable rule — not 'find the NNN-* dir,' which is ambiguous when multiple session-sets exist. Define one authoritative selector (e.g., the highest NNN- whose spec.md status field is not 'closed', or an explicit current-session pointer), enforce 'exactly one active set' in CI, and have start-here.md state that rule verbatim so a cold agent never guesses among sets.","risks_of_pick":"A second generated artifact can silently drift from the canonical tier doc or template if snapshot/CI coverage lapses; mitigate by generating start-here.md from the same bundle as the engine files and adding a drift check that diffs it against the rendered template on every CI run, plus a check that its tier-concept language only POINTS to the canonical doc rather than restating it.","why_not_the_other":"B minimizes file count but at the cost of the actual goal: pointing 'straight at spec.md' assumes spec.md carries the lifecycle procedure, which it does not — so B either re-duplicates the uniform procedure into every spec.md (worse drift than A's one generated file) or pushes the cold agent to the canonical URL (network/offline fragility and an extra hop with 'which set is active?' ambiguity). A's one generated, snapshot-tested file is a smaller and more controllable drift surface than per-set procedural duplication."}