# Consult round 18 — gemini-3.1-pro-preview, direct API (58 s)

**Recommendation:** The goal is achievable if bounded, but requires collecting all known human inputs at the start and restricting mid-session interruptions to structural impasses. · **Design:** Shift all checks for a credential, a remote, or a spending decision to the initial planning phase, leaving mid-session human escalations strictly for an unavailable AI service or an impasse the governor escalates as a "critical issue". · **The one risk:** The framework exhausts the AI budget retrying a terminal tree conflict (`cap-terminal-tree-moved`) that a human could have spotted immediately. · **Mitigation:** Enforce the orchestrator's strict cap on governor decisions per session so that bounded recovery always escalates to the human before excessive AI time is wasted. · **Delete:** Delete any mid-session prompts for context or decisions that can logically be collected at the start.

**Recommendation:** The manual Resume button can be removed entirely in favor of automated lifecycle rehydration. · **Design:** As the orchestrator proposes, the extension reads the session state on activation to continue an in-flight session automatically, while the dead AI CLI scenario is handled by the waiter's existing crash layer to restart the loop. · **The one risk:** An automatic rehydration mechanism gets stuck in an infinite crash-and-revive loop upon every editor reload. · **Mitigation:** Maintain the existing crash layer's limit of restarting twice per progress point before recording a terminal `crash` stop that breaks the cycle and escalates to the human. · **Delete:** Delete the manual "Resume Session" button from the extension UI entirely.

**Recommendation:** Delegate impasses to the orchestrator's closed-menu governor using the existing Auxiliary Reviewer, rather than granting an AI open execution powers. · **Design:** The framework invokes the Auxiliary Reviewer by handing it the stop record, the blocking rule, and a closed menu of actions ("waive this rule", "instruct the author", "hold the release", "escalate"), strictly executing the choice to waive process rules (e.g., file lists, step checks) but never evidence rules (`verification_clean`, `published_when_releasable`). · **The one risk:** The LLM selects invalid menu options or hallucinates unparseable text, stalling the governor itself. · **Mitigation:** The framework strictly parses the governor's menu selection, treating any deviation from the closed menu as an immediate escalation to the human. · **Delete:** Do not build the operator's proposal where a Verifying AI "operates like the framework" with open-ended exception powers.

**Recommendation:** Implement the orchestrator's strict state machine where every wait has an explicit owner and a timeout deadline. · **Design:** The UI must display precisely which of the three owners (the author owes an answer, a framework job is running, the human owes something) holds the token and for how long; if a deadline expires, the framework records an "instruction overdue" stop and enters the recovery ladder. · **The one risk:** Legitimate long-running framework jobs, such as massive test suites, trigger false-positive timeouts that abort valid runs. · **Mitigation:** Allow a running framework job or author to emit periodic heartbeats to extend its deadline before the framework forces a stop. · **Delete:** Delete the indefinite polling of framework jobs (defect 7) and the waiter's undocumented "no loop is driving" exit (defect 9).

**Recommendation:** Fix only the four specific defects (1, 2, 3, 5) that directly caused the beta failures, and rely on the new governor net to handle the rest. · **Design:** Execute manual remediation sessions to prioritize the checkout's release setting over declarations, enforce engine guards on `cancel`, ensure the loop checks session status beneath it, and remove the `next` command; defer the remaining audit findings to be fixed only if governor records prove they cause real-world stops. · **The one risk:** Unpatched edge cases, like the detached loop revival (defect 10), might continue to silently commit code outside the new timeout net. · **Mitigation:** The overarching timeout mechanism will trap these detached processes because the main visible loop will register as overdue, halting downstream progression. · **Delete:** Delete the operator's planned remediation sessions for bugs 4, 6, 7, 8, 9, and 10 to prevent over-engineering.

**Recommendation:** A single proof-experiment session patching the engine guards and establishing the timeout/owner net would have saved both beta tests. · **Design:** Block the AI engine from issuing `cancel --force` (preventing the Beta 1 uncommitted closure) and apply a universal deadline to all framework jobs and waiter actions (breaking the Beta 2 infinite wait), automatically routing both impasses into governed stops. · **The one risk:** The AI, blocked from cancelling a doomed session, endlessly loops trying to satisfy impossible release gates. · **Mitigation:** The new bounded recovery ladder limits retries, ensuring the governor either waives the blocking process rule or escalates the issue to a human. · **Delete:** Delete the absence of an engine guard on `close --force` and strictly restrict `cancel --force` access to human operators.

**Recommendation:** Aggressively prune redundant commands and hidden overrides that create parallel states and contradict the plan. · **Design:** Remove the typed `session declare` completely so the plan's checkout release setting remains the single source of truth (fixing defect 1), and remove `dabbler session next` to prevent it from stealing the lease and losing accepted answers (fixing defect 5). · **The one risk:** Developers lose the ability to manually override a release flag or nudge a stuck step if the automated framework stalls. · **Mitigation:** Developers dictate the release configuration entirely during the initial planning phase, and any mid-session stalls are automatically governed by the explicit owner/deadline UI. · **Delete:** Delete `session declare`, `session next`, and the waiter script's logic that revives the loop detached and hidden (defect 10).
