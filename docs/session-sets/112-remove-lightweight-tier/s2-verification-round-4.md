**VERIFIED** — I checked the fix delta against both ledgered blockers, searched the changed live docs for surviving current-tier/action claims, and verified the new oversized-evidence close-backstop path fails closed instead of tracing back.

- Fix verdict: L1 live user-facing docs still teach retired two-tier workflow/actions -- fix-accepted
- Fix verdict: L2 Marketplace README still teaches retired workflow/commands -- fix-accepted

**NITS**

- **Nit:** The new Copilot-seat path is still contradicted by unchanged prerequisite wording that says provider API keys/all three keys are required. It does not resurrect the removed tier or commands, but it can confuse keyless Copilot-seat users.