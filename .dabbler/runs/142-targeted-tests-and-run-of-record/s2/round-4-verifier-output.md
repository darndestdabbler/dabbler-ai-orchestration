**ISSUES FOUND**

The prior `selection_unknown`/no-smoke bypass is fixed in the gate, including the mixed mapped+unknown case. However, there is still one blocking workflow defect.

- **Issue 1:** The generated workflow still tells operators to run an unscoped affected-tests command, so remediation rounds can print the full suite before verification even though the gate now judges only the fix delta.
  - **Category:** Correctness
  - **Severity:** Major
  - **Evidence paths:** `ai_router/bootstrap.py:85`, `ai_router/affected.py:632`, `ai_router/affected.py:749`, `ai_router/test_evidence.py:613`, `ai_router/router-config.yaml:532`
  - **Failure scenario:** A normal remediation round follows a prior round that changed a declared repo-wide path such as `pyproject.toml`, `pytest.ini`, or `tests/conftest.py`, then the fix only changes a mapped source file. The operator follows the generated instruction `python -m ai_router.affected`; because no `--session-set-dir` is passed, `affected.main` measures against `HEAD` and can print `python -m pytest`. Recording that command is then judged against `preverify_baseline`, refused as a policy violation, and the workflow has recommended the full-suite preverify run it exists to eliminate. This is probable because blocking verification rounds/remediations are the core loop and repo-wide paths are first-class declared config.
  - **Acceptance criterion:** `JUDGMENT - Generated workflow text must make the affected-tests command use the same round-relative baseline as preverify_gate/test_evidence in remediation rounds, either by including --session-set-dir <dir> in the affected command or by making the unqualified affected command resolve the active session set equivalently.`
  - **Details:** **Violation:** the spec says to “print the targeted command before verification and the full-suite command only after the final verified tree.” **Impact:** a reasonable reviewer would not merge this because the documented main path still sends operators into an unnecessary/refused full-suite preverify run after a repo-wide earlier round. **Evidence:** `bootstrap.py` still emits `python -m ai_router.affected`, while `affected.py` only uses `preverify_baseline` when `--session-set-dir` is supplied, and both `preverify_gate`/`test_evidence` always judge against that session baseline.