**VERIFIED** — I traced the verifier candidate flow from `ai_router/route.py` through `ai_router/transports/copilot.py`, `ai_router/selection.py`, and `ai_router/identity.py`, against the shipped `ai_router/router-config.yaml`, and the round-1 Copilot trust gap is closed: verifier distrust now binds in shared `resolve_role()` via normalized model-token matching, and the shipped verifier preference no longer names the explicitly untrusted Gemini entry. I also checked `smoke.py` against `ai_router/config.py`; the script now forces `transport="api"`, so its probes are back to exercising provider APIs instead of the default seat path.