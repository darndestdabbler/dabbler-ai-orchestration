{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "router-config.yaml",
  "type": "object",
  "required": [
    "providers",
    "escalation"
  ],
  "properties": {
    "providers": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "object",
        "required": [
          "api_key_env",
          "rate_limit",
          "timeout_seconds",
          "retry"
        ],
        "properties": {
          "api_key_env": {
            "type": "string",
            "minLength": 1
          },
          "enabled": {
            "type": "boolean"
          },
          "display_label": {
            "type": "string"
          },
          "base_url": {
            "type": "string"
          },
          "api_version": {
            "type": "string"
          },
          "timeout_seconds": {
            "type": "number",
            "exclusiveMinimum": 0
          },
          "rate_limit": {
            "type": "object",
            "required": [
              "requests_per_minute",
              "tokens_per_minute"
            ],
            "properties": {
              "requests_per_minute": {
                "type": "integer",
                "exclusiveMinimum": 0
              },
              "tokens_per_minute": {
                "type": "integer",
                "exclusiveMinimum": 0
              }
            }
          },
          "retry": {
            "type": "object",
            "required": [
              "max_retries",
              "backoff_base_seconds"
            ],
            "properties": {
              "max_retries": {
                "type": "integer",
                "minimum": 0
              },
              "backoff_base_seconds": {
                "type": "number",
                "minimum": 0
              }
            }
          }
        }
      }
    },
    "provider_defaults": {
      "type": "object",
      "description": "What a direct-API dispatch needs that the catalog does not state: how much output to ask for, how large a prompt may get, which system prompt to send, and the provider's own generation knobs. There are NO MODEL NAMES in this block, which is the point of it -- a block with no model names in it cannot go stale the week a vendor ships a model nobody has added. The seat reads none of it: the CLI exposes no knobs.",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "max_context_tokens": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "description": "The SMALLEST window this provider serves: all it does is refuse an oversized prompt early."
          },
          "max_output_tokens": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "description": "This framework's output budget for the provider, well under every model's ceiling."
          },
          "system_prompt_file": {
            "type": "string",
            "description": "One consolidated file with an H2 section per provider slug; it was always read per provider."
          },
          "generation_params": {
            "type": "object"
          }
        }
      }
    },
    "roles": {
      "type": "object",
      "description": "Selection by role, applied identically on both transports. A role declares a preference order and a vehicle, and nothing else. 'prefer' is ORDERING ONLY -- a model the order does not name still qualifies and simply sorts after the named ones -- so a stale entry costs a slightly older model and never a candidate; names in it are model ids as each transport puts them on the wire, so a name that matches nothing on this path is inert rather than an error. What a person CHOSE is not here: a selection lives in this machine's user-level preferences beside the catalog, because which model reviews is a fact about who is at this keyboard and what their machine can reach.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "prefer": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "transport": {
            "type": "string",
            "enum": ["api", "copilot-cli", "offline"],
            "description": "This role's own vehicle: the transport it is dispatched over, where it differs from the machine's. A configured default, so a --transport flag and DABBLER_TRANSPORT still outrank it; a role that names none resolves exactly as the machine does. It exists because reviewer selection may need the other transport when provider independence requires it, which this framework stated long before any surface could act on it."
          }
        }
      }
    },
    "escalation": {
      "type": "object",
      "required": [
        "enabled",
        "max_escalations",
        "triggers"
      ],
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "max_escalations": {
          "type": "integer",
          "minimum": 0
        },
        "triggers": {
          "type": "object"
        },
        "refusal_phrases": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "transport": {
      "type": "object",
      "properties": {
        "profile": {
          "enum": [
            "api",
            "copilot-cli",
            "offline"
          ]
        }
      }
    },
    "transports": {
      "type": "object",
      "properties": {
        "offline": {
          "type": "object",
          "properties": {
            "responses_dir": {
              "type": "string",
              "minLength": 1,
              "description": "Directory of scripted verifier responses, consumed in lexical order. Overridden by DABBLER_OFFLINE_RESPONSES."
            }
          }
        },
        "copilot-cli": {
          "type": "object",
          "properties": {
            "binary": {
              "type": "string",
              "minLength": 1
            },
            "billed_usage_unavailable": {
              "type": "boolean"
            },
            "max_invocations_per_session": {
              "type": "integer",
              "exclusiveMinimum": 0
            },
            "timeouts": {
              "type": "object",
              "properties": {
                "spawn_seconds": {
                  "type": "number",
                  "exclusiveMinimum": 0
                },
                "first_byte_seconds": {
                  "type": "number",
                  "exclusiveMinimum": 0
                },
                "total_seconds": {
                  "type": "number",
                  "exclusiveMinimum": 0
                }
              }
            }
          }
        }
      }
    },
    "task_type_params": {
      "type": "object",
      "description": "Per task type, the generation knobs that task wants, keyed by PROVIDER. These were always provider-shaped: effort and thinking are Anthropic vocabulary, reasoning_effort OpenAI, thinking_budget Google."
    },
    "testing": {
      "type": "object",
      "description": "Suites, deterministic controls, and the path-to-test selection rules. A repository declares this in its own tracked dabbler.yaml; the block appears here because that file is deep-merged onto this one and the merged result is validated as a whole."
    },
    "paths": {
      "type": "object",
      "additionalProperties": false,
      "description": "Path facts about the repository, declared in its tracked dabbler.yaml. Deliberately not part of run_policy: the machine-local overlay may override run policy and may not override this.",
      "properties": {
        "sensitive_paths": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    },
    "packaging": {
      "type": "object",
      "description": "Step (f) of the session lifecycle: pack, then push to the feed. A repository that declares no packaging block publishes nothing, which is a declaration rather than an omission -- there is no inferred build here, because guessing how a repository publishes is guessing an ecosystem. Placeholders are substituted per argv element and never through a shell: {output} is the run's own output directory, {artifact} is one file pack produced, {feed} is the declared feed, and {secret} is the resolved credential. Both commands are argv, not shell strings, so a credential cannot be re-split by a shell, and both are spawned with the child-environment allowlist, so the credential is in no environment at all. A repository whose release is a tag declares release: tag instead of the pair, and the two are mutually exclusive.",
      "additionalProperties": false,
      "properties": {
        "pack": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "argv"
          ],
          "properties": {
            "argv": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "string",
                "minLength": 1
              },
              "description": "Must contain {output}. A pack that does not take its output directory from the framework writes into the repository, dirtying the tree that was just verified and leaving last week's build sitting where this week's push will find it."
            },
            "cwd": {
              "type": "string"
            },
            "timeout_seconds": {
              "type": "number",
              "exclusiveMinimum": 0
            }
          }
        },
        "push": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "argv",
            "feed"
          ],
          "properties": {
            "argv": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "string",
                "minLength": 1
              },
              "description": "Must contain {artifact} and {feed}, and {secret} whenever the feed takes a credential. The first two are required rather than conventional: a command without {artifact} pushes nothing the framework can name, and a command without {feed} makes the recorded destination a label instead of a fact about what ran. {secret} is required for every feed that authenticates -- a command without it there is publishing on an ambient credential the framework cannot see, which makes 'the PAT is never in an environment' unprovable rather than true -- and is left out for a feed that is a folder on disk, which authenticates nothing. Which is which is decided by the loader from the feed, not declared here."
            },
            "feed": {
              "type": "string",
              "minLength": 1,
              "description": "A URL, or a folder on disk -- a drive path, a UNC path, a POSIX path or file://. A folder takes no credential."
            },
            "secret": {
              "type": "string",
              "minLength": 1,
              "description": "The NAME of the credential, never its value. It resolves through the secret resolver at spawn time, exactly as a provider's api_key_env does. Required by the loader when the feed takes a credential; a folder feed declares none."
            },
            "secret_source": {
              "type": "string",
              "minLength": 1,
              "description": "Which secret_resolver backend holds it. Defaults to 'env'."
            },
            "cwd": {
              "type": "string"
            },
            "timeout_seconds": {
              "type": "number",
              "exclusiveMinimum": 0
            }
          }
        },
        "release": {
          "const": "tag",
          "description": "This repository releases by TAG rather than from this machine: dabbler release pushes an annotated vsix-v* tag and a tag-driven CI workflow publishes from it with a credential no session ever sees. Declared instead of pack/push, never beside them -- a repository releases one way, and a block claiming both leaves the record unable to say which one it describes. The packaging run then RECORDS whether the tag reached origin; it never makes it, because a tag is the one act that waits on the operator publication decision rather than on a phase advancing."
        }
      },
      "oneOf": [
        {
          "required": [
            "release"
          ],
          "not": {
            "anyOf": [
              {
                "required": [
                  "pack"
                ]
              },
              {
                "required": [
                  "push"
                ]
              }
            ]
          }
        },
        {
          "required": [
            "pack",
            "push"
          ],
          "not": {
            "required": [
              "release"
            ]
          }
        }
      ]
    },
    "verification": {
      "type": "object"
    },
    "critique": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "pipeline": {
          "enum": [
            "off",
            "shadow",
            "enforce"
          ],
          "description": "The critique pipeline's authority. 'off' is the default and writes nothing. 'shadow' records critique artifacts without letting them decide anything. 'enforce' is declared here so the vocabulary lives in one place, and is refused at load until the set that implements it lands — see config.py, which names that set in the refusal."
        }
      }
    },
    "metrics": {
      "type": "object"
    },
    "metadata": {
      "type": "object"
    },
    "run_policy": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "default": {
          "enum": [
            "fast",
            "verified"
          ]
        },
        "verification_rounds": {
          "type": "integer",
          "minimum": 1
        },
        "diff_limit_lines": {
          "type": "integer",
          "minimum": 1
        },
        "check_timeout_seconds": {
          "type": "number",
          "exclusiveMinimum": 0
        },
        "budgets": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "model_usd": {
              "type": [
                "number",
                "null"
              ],
              "exclusiveMinimum": 0
            },
            "model_dispatches": {
              "type": "integer",
              "minimum": 1
            },
            "elapsed_minutes": {
              "type": [
                "number",
                "null"
              ],
              "exclusiveMinimum": 0
            }
          }
        }
      }
    },
    "git": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "push_on_finish": {
          "type": "boolean"
        },
        "worktree_per_run": {
          "type": "boolean"
        },
        "remote": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "explorer": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "stale_after_minutes": {
          "type": "number",
          "exclusiveMinimum": 0
        }
      }
    },
    "discovery": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "key_set_id": {
          "type": "string",
          "minLength": 1
        },
        "max_age_hours": {
          "type": "number",
          "exclusiveMinimum": 0
        }
      }
    },
    "driver": {
      "type": "object",
      "additionalProperties": false,
      "description": "How far `dabbler session drive` may go. Declared in the repository's dabbler.yaml (it is a repository-owned block) and read here after the merge.",
      "properties": {
        "max_invocations": {
          "type": "integer",
          "minimum": 1
        },
        "engine_output": {
          "enum": [
            "stream",
            "quiet"
          ]
        }
      }
    },
    "worktree": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "root": {
          "type": [
            "string",
            "null"
          ]
        },
        "init": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "id"
            ],
            "properties": {
              "id": {
                "type": "string",
                "minLength": 1
              },
              "argv": {
                "oneOf": [
                  {
                    "type": "array",
                    "minItems": 1,
                    "items": {
                      "type": "string"
                    }
                  },
                  {
                    "type": "object",
                    "additionalProperties": false,
                    "required": [
                      "windows",
                      "posix"
                    ],
                    "properties": {
                      "windows": {
                        "type": "array",
                        "minItems": 1,
                        "items": {
                          "type": "string"
                        }
                      },
                      "posix": {
                        "type": "array",
                        "minItems": 1,
                        "items": {
                          "type": "string"
                        }
                      }
                    }
                  }
                ]
              },
              "probe_argv": {
                "oneOf": [
                  {
                    "type": "array",
                    "minItems": 1,
                    "items": {
                      "type": "string"
                    }
                  },
                  {
                    "type": "object",
                    "additionalProperties": false,
                    "required": [
                      "windows",
                      "posix"
                    ],
                    "properties": {
                      "windows": {
                        "type": "array",
                        "minItems": 1,
                        "items": {
                          "type": "string"
                        }
                      },
                      "posix": {
                        "type": "array",
                        "minItems": 1,
                        "items": {
                          "type": "string"
                        }
                      }
                    }
                  }
                ]
              },
              "shell": {
                "type": "boolean"
              },
              "command": {
                "type": "string",
                "minLength": 1
              },
              "timeout_seconds": {
                "type": "number",
                "exclusiveMinimum": 0
              }
            }
          }
        }
      }
    }
  }
}
