**VERIFIED** — I traced the round-2 failure path from `ai_router.discovery.cmd_enumerate()` through `write_record()` into `ai_router.lockfile.write_document()`, and the defect is resolved: the sanctioned writer now creates the parent directory before writing, so the default `.dabbler/api-models.lock` can be created on a fresh checkout. I also checked the new regression test in `tests/test_discovery.py` and the surrounding freshness/session code it touches, and I did not find a new blocking defect in this delta.