{
  "schemaVersion": 1,
  "sessionNumber": 2,
  "verificationRound": 1,
  "verificationVerdict": "ISSUES_FOUND",
  "issues": [
    {
      "description": "** The retention report ignores live guidance IDs that are missing from the ledger, so the cap and disuse report can silently undercount the corpus.\n  - **Category:** Correctness\n  - **Severity:** Major\n  - **Evidence paths:** `ai_router/guidance_ledger.py:679`, `ai_router/guidance_ledger.py:811`, `docs/session-sets/121-guidance-becomes-executable/spec.md:321`\n  - **Failure scenario:** A future session adds/registers live guidance IDs imperfectly — especially Session 3’s planned project-guidance ID admission — and one live instruction marker is missing from `guidance-usage.json`. That is probable because registration is a separate state write, and the current report has no reverse check. The operator then runs `guidance_ledger report`; the unregistered instruction is neither counted against `instruction_line_cap` nor surfaced as never-used/missing, so the anti-rebloat mechanism can pass while the live corpus has already exceeded its evidence-backed cap.\n  - **Acceptance criterion:** JUDGMENT - With a live guidance corpus containing an ID absent from `guidance-usage.json`, `guidance_ledger report` must count that ID against `instruction_line_cap` and surface a missing-ledger/never-used problem instead of silently treating the corpus as smaller.\n  - **Details:** **Violation:** the spec requires “one compact record per guidance item” and ends with guidance prunable “deliberately on evidence of disuse.” **Impact:** missing ledger records become invisible to the only retention/cap report, which materially impairs the session’s core objective. **Evidence:** `retention_report()` iterates only `ledger.entries`, and `instruction_count()` also counts only ledger entries filtered by `governed_ids`; `corpus_ids()` is never used to add or fail missing live IDs.",
      "category": "Correctness",
      "severity": "Major",
      "failureScenario": "A future session adds/registers live guidance IDs imperfectly — especially Session 3’s planned project-guidance ID admission — and one live instruction marker is missing from `guidance-usage.json`. That is probable because registration is a separate state write, and the current report has no reverse check. The operator then runs `guidance_ledger report`; the unregistered instruction is neither counted against `instruction_line_cap` nor surfaced as never-used/missing, so the anti-rebloat mechanism can pass while the live corpus has already exceeded its evidence-backed cap.",
      "evidencePaths": [
        "ai_router/guidance_ledger.py",
        "docs/session-sets/121-guidance-becomes-executable/spec.md"
      ],
      "acceptance": {
        "kind": "judgment",
        "statement": "With a live guidance corpus containing an ID absent from `guidance-usage.json`, `guidance_ledger report` must count that ID against `instruction_line_cap` and surface a missing-ledger/never-used problem instead of silently treating the corpus as smaller."
      },
      "discoveryCall": 1,
      "discoveryLens": "spec-conformance"
    },
    {
      "description": "** The canonical workflow’s Step 9 archival guidance still instructs operators to use the retired `last-used-set` / `guidance.disuse_window_sets` rule.\n  - **Category:** Completeness\n  - **Severity:** Major\n  - **Evidence paths:** `docs/ai-led-session-workflow.md:2448`, `ai_router/guidance_meta.py:238`, `ai_router/guidance_ledger.py:967`\n  - **Failure scenario:** On the final session of any future set, the orchestrator opens the workflow Step 9 procedure and follows the still-present “no `last-used-set` activity for default 20 sets” rule. That path is likely because Step 9 is mandatory and this document remains the procedure reference. Since `last-used-set` is no longer written, the operator is directed to stale evidence instead of the ledger’s active-session retention report.\n  - **Acceptance criterion:** JUDGMENT - The Step 9 archival/reorganization procedure in `docs/ai-led-session-workflow.md` no longer tells operators to use `last-used-set` or `guidance.disuse_window_sets`, and instead points to `guidance_ledger report`, active-session retention, and batched operator-initiated pruning.\n  - **Details:** **Violation:** Session 2 requires retention by active sessions and moving usage out of preload; the implementation retires `update_last_used()` and derives `instruction_window_sessions = 30`. **Impact:** the repo would ship contradictory operational instructions on the exact pruning decision this session is meant to make evidence-based. **Evidence:** the workflow still says archival is based on “no `last-used-set` activity” and “default 20 sets,” while the code now raises on `update_last_used()` and uses the ledger’s active-session settings.",
      "category": "Completeness",
      "severity": "Major",
      "failureScenario": "On the final session of any future set, the orchestrator opens the workflow Step 9 procedure and follows the still-present “no `last-used-set` activity for default 20 sets” rule. That path is likely because Step 9 is mandatory and this document remains the procedure reference. Since `last-used-set` is no longer written, the operator is directed to stale evidence instead of the ledger’s active-session retention report.",
      "evidencePaths": [
        "docs/ai-led-session-workflow.md",
        "ai_router/guidance_meta.py",
        "ai_router/guidance_ledger.py"
      ],
      "acceptance": {
        "kind": "judgment",
        "statement": "The Step 9 archival/reorganization procedure in `docs/ai-led-session-workflow.md` no longer tells operators to use `last-used-set` or `guidance.disuse_window_sets`, and instead points to `guidance_ledger report`, active-session retention, and batched operator-initiated pruning."
      },
      "discoveryCall": 1,
      "discoveryLens": "spec-conformance"
    },
    {
      "description": "** Never-used live guidance items are invisible to retention reporting and the instruction cap.\n- **Category:** Correctness\n- **Severity:** Major\n- **Evidence paths:** `ai_router/guidance_ledger.py:679`, `ai_router/guidance_ledger.py:811`, `test-fixtures/cold-start/full/docs/planning/lessons-learned.md:68`\n- **Failure scenario:** A new lesson or project-guidance entry gets an id but has not yet been cited. That is probable because new guidance is created before it has usage history, and Session 3 is explicitly about admitting currently unidentified project-guidance entries. The report iterates/counts only existing ledger entries, so the new live instruction is not reported as `never-used` and does not count against the cap.\n- **Acceptance criterion:** `python ai_router/guidance_ledger.py --repo-root test-fixtures/cold-start/full report`\n- **Acceptance expectation:** exit 0, output contains `\"instruction lines in the live corpus: 1 / cap\"`\n- **Details:** **Violation:** the task requires “one compact record per guidance item” and retention based on whether an instruction was used within the last N active sessions. **Impact:** the anti-rebloat mechanism misses the exact case it must govern: new or never-used instruction lines. **Evidence:** `retention_report()` loops over `ledger.entries`, and `instruction_count()` counts instruction entries in the ledger; the cold-start fixture has a live `L-001-1` marker but no ledger entry, so today it is counted as zero.",
      "category": "Correctness",
      "severity": "Major",
      "failureScenario": "A new lesson or project-guidance entry gets an id but has not yet been cited. That is probable because new guidance is created before it has usage history, and Session 3 is explicitly about admitting currently unidentified project-guidance entries. The report iterates/counts only existing ledger entries, so the new live instruction is not reported as `never-used` and does not count against the cap.",
      "evidencePaths": [
        "ai_router/guidance_ledger.py",
        "test-fixtures/cold-start/full/docs/planning/lessons-learned.md"
      ],
      "acceptance": {
        "kind": "executable",
        "command": "python ai_router/guidance_ledger.py --repo-root test-fixtures/cold-start/full report",
        "expectedExitCode": 0,
        "expectedOutputContains": "\"instruction lines in the live corpus: 1 / cap\""
      },
      "discoveryCall": 2,
      "discoveryLens": "failure-scenario"
    },
    {
      "description": "** The implementation is not ready for `project-guidance.md` ids, despite that being a load-bearing requirement.\n- **Category:** Completeness\n- **Severity:** Major\n- **Evidence paths:** `ai_router/cite_lessons.py:197`, `ai_router/guidance_meta.py:81`, `ai_router/guidance_ledger.py:628`, `docs/planning/project-guidance.md:21`\n- **Failure scenario:** Session 3 assigns ids such as `C-003` to project-guidance entries. `cite_lessons` searches only `lessons-learned.md` and `lessons-archive.md`, `guidance_meta` still validates only `L-...` ids, and corpus scanning reuses the level-2 lesson parser against a document whose real entries are lower-level sections. Citation/validation/reporting then fail or ignore those ids.\n- **Acceptance criterion:** `JUDGMENT - A reviewer can see tests and product code proving that a non-L project-guidance marker is validated, discovered in the live corpus, and resolved by cite_lessons without changing the ledger format.`\n- **Details:** **Violation:** the task says the ledger is “agnostic about which document an entry lives in” and `project-guidance.md` “needs the identical mechanism.” **Impact:** the next planned session cannot use the mechanism without more code changes, so this session has not delivered the promised readiness. **Evidence:** `cite_lessons` excludes `PROJECT_GUIDANCE`; `ID_RE` rejects the example namespace; `corpus_ids()` includes `PROJECT_GUIDANCE` only through the lesson parser.",
      "category": "Completeness",
      "severity": "Major",
      "failureScenario": "Session 3 assigns ids such as `C-003` to project-guidance entries. `cite_lessons` searches only `lessons-learned.md` and `lessons-archive.md`, `guidance_meta` still validates only `L-...` ids, and corpus scanning reuses the level-2 lesson parser against a document whose real entries are lower-level sections. Citation/validation/reporting then fail or ignore those ids.",
      "evidencePaths": [
        "ai_router/cite_lessons.py",
        "ai_router/guidance_meta.py",
        "ai_router/guidance_ledger.py",
        "docs/planning/project-guidance.md"
      ],
      "acceptance": {
        "kind": "judgment",
        "statement": "A reviewer can see tests and product code proving that a non-L project-guidance marker is validated, discovered in the live corpus, and resolved by cite_lessons without changing the ledger format."
      },
      "discoveryCall": 2,
      "discoveryLens": "failure-scenario"
    },
    {
      "description": "** `cite_lessons` records unknown ids into the ledger before reporting them as not found.\n- **Category:** Correctness\n- **Severity:** Major\n- **Evidence paths:** `ai_router/cite_lessons.py:212`, `ai_router/cite_lessons.py:247`, `ai_router/guidance_ledger.py:408`\n- **Failure scenario:** An operator mistypes an id during close. That is probable enough that the CLI has an explicit not-found path. The command exits non-zero, but it has already created a syntactically valid ghost ledger entry; a later rerun with the correct id leaves the false record behind, and the module deliberately has no eviction path.\n- **Acceptance criterion:** `JUDGMENT - Citing an id not present in any guidance document leaves guidance-usage.json byte-identical and reports the id as not recorded.`\n- **Details:** **Violation:** the ledger is supposed to hold records for guidance items, not typo strings. **Impact:** the usage ledger becomes false evidence and can later attach prior “uses” to an id that did not exist when cited. **Evidence:** `cite_lessons.main()` calls `record_citation(args.ids, ...)` before checking outcomes, `_record()` creates missing entries, and the not-found message says “The use was still recorded in the ledger.”",
      "category": "Correctness",
      "severity": "Major",
      "failureScenario": "An operator mistypes an id during close. That is probable enough that the CLI has an explicit not-found path. The command exits non-zero, but it has already created a syntactically valid ghost ledger entry; a later rerun with the correct id leaves the false record behind, and the module deliberately has no eviction path.",
      "evidencePaths": [
        "ai_router/cite_lessons.py",
        "ai_router/guidance_ledger.py"
      ],
      "acceptance": {
        "kind": "judgment",
        "statement": "Citing an id not present in any guidance document leaves guidance-usage.json byte-identical and reports the id as not recorded."
      },
      "discoveryCall": 2,
      "discoveryLens": "failure-scenario"
    },
    {
      "description": "** `--session` is optional and silently defaults to session 1.\n- **Category:** Correctness\n- **Severity:** Major\n- **Evidence paths:** `ai_router/cite_lessons.py:175`\n- **Failure scenario:** A session 2+ close uses the old command form or omits `--session`. Multi-session sets are normal in this repo, and the old invocation was the established workflow. The ledger records `<set>-01`, corrupting per-session usage history without any error.\n- **Acceptance criterion:** `python ai_router/cite_lessons.py --set 121 L-079-1 --repo-root .`\n- **Acceptance expectation:** exit 2\n- **Details:** **Violation:** uses must be identified as `<set>-<session>`, and the updated documented command includes `--session <M>`. **Impact:** the main citation path can silently write the wrong active-session label, undermining the retention window. **Evidence:** the parser sets `default=\"1\"` instead of requiring the session number.",
      "category": "Correctness",
      "severity": "Major",
      "failureScenario": "A session 2+ close uses the old command form or omits `--session`. Multi-session sets are normal in this repo, and the old invocation was the established workflow. The ledger records `<set>-01`, corrupting per-session usage history without any error.",
      "evidencePaths": [
        "ai_router/cite_lessons.py"
      ],
      "acceptance": {
        "kind": "executable",
        "command": "python ai_router/cite_lessons.py --set 121 L-079-1 --repo-root .",
        "expectedExitCode": 2
      },
      "discoveryCall": 2,
      "discoveryLens": "failure-scenario"
    },
    {
      "description": "** The shipped ledger contains usage labels contradicted by the close-event history it claims to replay.\n- **Category:** Correctness\n- **Severity:** Major\n- **Evidence paths:** `docs/planning/guidance-usage.json:36`, `docs/session-sets/122-module-lifecycle-to-python/session-events.jsonl:5`, `ai_router/guidance_ledger.py:825`\n- **Failure scenario:** The current repo’s retention report and triage prompt consume `guidance-usage.json` as historical truth. It already contains at least one false label: `L-064-12` records `122-02`, but Set 122 session 2 did not cite that id; session 3 did. Any item near a window boundary can be retained or pruned from fabricated recency evidence.\n- **Acceptance criterion:** `JUDGMENT - guidance-usage.json is regenerated or validated so every instruction use label appears only when that session's closeout_succeeded event contains the id; specifically L-064-12 no longer has 122-02 and does include the correct 122-03 history if retained by the ring.`\n- **Details:** **Violation:** the module says backfill is from “every session’s close event” and ordered as true recency. **Impact:** the session’s recorded basis for N/cap and future pruning is not trustworthy. **Evidence:** the ledger lists `L-064-12` with `122-02`; the corresponding close event cites only `L-112-1`, `L-069-1`, and `L-066-1`, while `L-064-12` appears in session 3.",
      "category": "Correctness",
      "severity": "Major",
      "failureScenario": "The current repo’s retention report and triage prompt consume `guidance-usage.json` as historical truth. It already contains at least one false label: `L-064-12` records `122-02`, but Set 122 session 2 did not cite that id; session 3 did. Any item near a window boundary can be retained or pruned from fabricated recency evidence.",
      "evidencePaths": [
        "docs/planning/guidance-usage.json",
        "docs/session-sets/122-module-lifecycle-to-python/session-events.jsonl",
        "ai_router/guidance_ledger.py"
      ],
      "acceptance": {
        "kind": "judgment",
        "statement": "guidance-usage.json is regenerated or validated so every instruction use label appears only when that session's closeout_succeeded event contains the id; specifically L-064-12 no longer has 122-02 and does include the correct 122-03 history if retained by the ring."
      },
      "discoveryCall": 2,
      "discoveryLens": "failure-scenario"
    }
  ],
  "phase": "discovery",
  "discoveryBaselineTree": "147356067c9b584cdd2fa690d208a13cf285764d"
}
