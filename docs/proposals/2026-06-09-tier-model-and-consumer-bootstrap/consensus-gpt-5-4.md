{
  "tier_model_ok": true,
  "engine_files": "Write AGENTS.md, CLAUDE.md, and GEMINI.md in every consumer repo, always. Do not optimize for \"engines used\"; a cold orchestrator may be a different engine next session, and each engine auto-reads only its own repo-root file. Use thin bootstrap files, not inline hand-maintained copies. Each file should have the same shared body plus the minimal engine-specific tail. The shared body must say: this repo uses Dabbler; first open docs/dabbler/start-here.md; treat docs/session-sets/<slug>/spec.md as authoritative; read tier and verificationMode from spec.md; tier: lightweight means router off / zero external LLM API calls, not no Python and not no close-out; both tiers use the same Python lifecycle. Also include a stable canonical raw GitHub URL for background, but the operative instructions must exist locally via docs/dabbler/start-here.md so self-orientation does not depend on network access. The Lightweight body must explicitly say: same package, same venv, same start_session/close_session flow, same writer-discipline layer, same close-out gate; only the router is disabled.",
  "setup_uniformity": "Make setup uniform. Both tiers should scaffold a venv and pip install the package. The only intentional setup difference should be that Full also writes router/config guidance while Lightweight sets tier: lightweight in spec.md and omits router credentials. Everything else should stay identical: package install, CLI/lifecycle availability, engine files, folder layout, start/close flow, and close-out expectations. Keeping Lightweight 'Python-off' is exactly the stale model that caused the operator gap; the codebase says otherwise.",
  "spec_template": "Use one canonical spec.md template for both tiers, with schemaVersion 4 and fixed section order. Required fields: schemaVersion: 4; slug: NNN-kebab-case; title; tier: full|lightweight; verificationMode: dedicated-sessions|out-of-band-or-none; objective/summary; scope; constraints; deliverables; completionDefinition. Keep verificationMode present in both tiers for schema uniformity; default it to out-of-band-or-none unless the operator explicitly chooses dedicated-sessions. The generator fix is not a wording tweak; replace the ad hoc markdown assembly in sessionGenPrompt.ts with one packaged canonical template writer shared by the fast path, the wizard, and gitScaffold. That writer must always emit schemaVersion 4, enforce NNN- slug validation, always include tier, always include verificationMode, and never emit the old schemaVersion: 2 shape again.",
  "cold_start": "Deterministic mechanism: the orchestrator first auto-reads its repo-root engine file. That file's first real instruction must be: 'Open docs/dabbler/start-here.md now.' docs/dabbler/start-here.md then tells it exactly what to do: activate/create .venv if needed, ensure the Dabbler package is installed, locate the active/next docs/session-sets/NNN-*/spec.md, read tier and verificationMode from that spec, and run the session accordingly. If tier=full, use the normal routed lifecycle. If tier=lightweight, use the same lifecycle in --no-router/manual-attestation mode and make zero external LLM API calls. In both cases, close the session through the shared close-out gate. In short: engine file first, then local start-here, then spec.md. That is the cold-start chain that removes 'what do I do next?' guessing.",
  "fix_location": "BOTH. Put the normative model, spec template, and bootstrap text in canonical docs/templates, and change the extension/scaffolder to materialize the right artifacts on every entry path, including the fast path. Code changes are mandatory because the current wizard, sessionGenPrompt.ts, and gitScaffold.ts literally generate stale/broken Lightweight repos. Docs/template changes are also mandatory because you need a durable source of truth for humans and AI and you cannot keep prose duplicated in extension code without drift. The extension should write generic stubs from the canonical contract; it should not become the source of truth.",
  "doc_reconciliation": "Use a single source of truth plus pointers. Define one canonical bootstrap contract for the tier model and startup rules, and one canonical spec template. README, docs/adoption-bootstrap.md, and the Get Started wizard should summarize only the essentials and point to that canonical source; they should not each restate the tier model in their own words. Consumer engine files should point first to the generated local docs/dabbler/start-here.md, and that local file should cite the canonical raw URL. Generate or snapshot-test the engine stubs, wizard copy fragments, and spec template from the same contract/template bundle, and add CI drift checks so wording like 'Lightweight = no Python' cannot reappear unnoticed.",
  "sequencing": "1) Ratify the canonical bootstrap contract: corrected tier model, v4 spec template, engine bootstrap text, and docs/dabbler/start-here.md template. 2) Fix sessionGenPrompt.ts to use the shared template writer and enforce schemaVersion 4, NNN slugs, tier, and verificationMode. 3) Fix gitScaffold.ts to scaffold .venv/bootstrap commands, install the package, write docs/dabbler/start-here.md, and write AGENTS.md/CLAUDE.md/GEMINI.md in all repos; add router/config only for Full. 4) Update the Get Started wizard so prerequisites, copy, and generated outputs match the same contract and end with an explicit 'you are now ready; tell your orchestrator start the next session' closure. 5) Rewrite README and docs/adoption-bootstrap.md as thin summaries/pointers to the canonical model. 6) Add acceptance tests and CI: cold-start from each engine file, both tiers, no-router behavior, generator snapshots, drift checks. 7) Ship the Marketplace release and backfill existing consumer repos.",
  "confidence": "high",
  "key_risks": "The main risk is creating a new local bootstrap file (docs/dabbler/start-here.md) and then letting it drift from the canonical docs; solve that by generating it from the same template bundle and snapshot-testing it. Second risk: verificationMode can confuse operators if its role in Full is not clearly marked as schema-uniform but operationally secondary. Third risk: docs may land before the Marketplace release, temporarily widening the gap unless you sequence release and communication tightly.",
  "strongest_dissent": "The best argument against this recommendation is that a docs-only fix would be faster and would avoid a Marketplace release, because runtime_mode.py already supports Lightweight correctly once spec.md is written properly. That argument is real, but I still reject it: the current fast path and wizard are the exact places new repos get created, and they currently encode the wrong model. If you do not fix the extension paths as well, you will keep generating repos that fail the cold-start acceptance test."
}