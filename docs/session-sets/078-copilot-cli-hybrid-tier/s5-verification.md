- **No issue** → `ai_router/cli_transport.py:601-609`, `ai_router/tests/test_cli_transport.py:436-450` → The S5 fix is correct and complete. It closes the missing-`data` false-success path by requiring key presence before payload extraction; the existing `isinstance(message_data, dict)` guard still preserves the sibling present-but-malformed-`data` failure path, and the present-but-incomplete dict behavior remains unchanged.

- **Issue** → `ai_router/CHANGELOG.md` (`0.28.0` / Fixed bullet for assistant-message `data`) → The prose no longer matches the shipped code. It says the fix was `final_message.get("data", {})`, but the final implementation explicitly rejects an absent `data` key. Rewrite this bullet to describe the actual final behavior: `assistant.message.data` is required, unwrapped before field extraction, and missing/non-dict `data` fails closed as malformed.

- **Issue** → `ai_router/CHANGELOG.md` (`Rollback`, step 3) and `docs/repository-reference.md` (`0.28.0` recent version walk rollback sentence) → Both recommend `pip install dabbler-ai-router==0.27.0`, but the same session state says `0.27.0` is still publish-pending and not yet available on PyPI. Qualify that rollback path as valid only after the pending `v0.27.0` publish/tag, or give a currently-valid source/commit rollback path.

- **Issue** → `docs/templates/consumer-bootstrap/engine-file.agents-tail.md` → “Want full Full-tier guarantees on a Copilot-only seat” overclaims the feature and contradicts the rest of the docs, which correctly describe this as an indirect Full tier with explicitly degraded guarantees. Rephrase to something like “Want the Full-tier workflow path on a Copilot-only seat?” or equivalent.

- **Issue** → `docs/repository-reference.md` (`0.28.0` recent version walk, final sentence “No extension changes.”) → That sentence is too absolute. This session changed the canonical consumer-bootstrap template and required a recompiled extension dist mirror, so extension-generated scaffold output did change. Soften it to “No extension version bump / no new extension feature work beyond the mirrored bootstrap-template doc pointer” or remove the sentence.

- **No issue** → `pyproject.toml`, `ai_router/CHANGELOG.md`, `docs/repository-reference.md` → Release-order handling is correct. The repo is version-bumped and documented as publish-pending, the required `v0.27.0` then `v0.28.0` sequence is stated, and there is no improper claim that either tag or a PyPI publish already happened.

- **No issue** → `docs/session-sets/078-copilot-cli-hybrid-tier/s5-issues.json` → The google/gemini `no_change_needed` adjudication is justified. The finding re-reports a previously identified fabricated review artifact, not an unresolved defect in this session’s shipped code/docs, and leaving the raw artifact untouched matches the stated artifact-immutability rule.