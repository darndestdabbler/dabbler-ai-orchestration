{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "disputes.jsonl row (one disputed finding)",
  "description": "One row per disputed verification finding in .dabbler/runs/<set>/s<N>/disputes.jsonl. Written only by the CLI (ai_router.verify dispute); the next round's prompt and the adjudicator read it. A row that fails this schema on read is a refusal, never a skip.",
  "type": "object",
  "required": [
    "round",
    "finding_index",
    "filed_after_round",
    "grounds",
    "evidence_paths",
    "recorded_at"
  ],
  "properties": {
    "round": { "type": "integer", "minimum": 1 },
    "finding_index": { "type": "integer", "minimum": 0 },
    "filed_after_round": { "type": "integer", "minimum": 1 },
    "grounds": { "type": "string", "minLength": 1 },
    "evidence_paths": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 }
    },
    "recorded_at": { "type": "string", "minLength": 1 }
  }
}
