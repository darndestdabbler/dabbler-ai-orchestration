ISSUES FOUND — The workspace-root cap lookup and the `reviewRounds` / `lastLiveReview` preservation fix themselves look right, but same-step `enter` still clears the current review state while preserving the old terminal review, which leaves a common same-step path incoherent.

- **Issue 1:** Same-step `enter` can leave a step neither approvable nor re-reviewable on the same step after a clear live review.
  - **Category:** Correctness
  - **Severity:** Major
  - **Evidence paths:** `ai_router/workflow.py:16-29`, `ai_router/workflow.py:179-191`, `ai_router/workflow.py:248-262`, `ai_router/workflow.py:489-503`, `ai_router/solution.py:91-95`
  - **Failure scenario:** After a clear live review on `plan` or `decompose`, an author edits the artifact and runs `workflow enter <current-step>` — the exact same-step path the prior round already identified as the natural thing to do after editing current-step work. The fold clears `reviewed`, `waitingOn`, `findings`, and `reviewers`, so `workflow approve` is refused and the projection stops showing the pending sign-off; but `lastLiveReview` is preserved, so `review_terminal()` still returns `VERIFIED` and `workflow review` is refused too. A routine same-step re-entry therefore leaves the step in a self-contradictory state unless the operator performs an artificial step change.
  - **Acceptance criterion:** `JUDGMENT - After a clear live review on plan or decompose, a same-step entered event no longer leaves folded state with reviewed == False while review_terminal(...) is already VERIFIED; same-step re-entry is either fully inert or reopens the loop consistently so approve and review do not both refuse for opposite reasons.`
  - **Details:** **Violation:** the new contract says same-step re-entry "moves nothing," but the `entered` handler still clears `reviewed`, `approved`, `waitingOn`, `findings`, and `reviewers` on every same-step enter. **Impact:** because `review_terminal()` is driven only by the preserved `lastLiveReview`, while `approve` still requires `reviewed` on approval steps, this change trades the old cap-bypass for a common state where the step's current review state is cleared but the old terminal review still blocks a new round. That is material enough to change a merge decision. **Evidence:** the state reset is in `fold()`; terminal closure is in `review_terminal()`; approval gating is in `validate_transition()`; and `plan` / `decompose` are the approval steps.

**NITS**

- **Nit:** The Round 1 status nit only partially cleared: `status` now shows open-loop counts once `reviewRounds` is non-zero, but both print sites still guard on `reviewRounds` truthiness (`ai_router/workflow.py:683-692`), so an entered-but-unreviewed step still hides `open, 0/<cap>`.