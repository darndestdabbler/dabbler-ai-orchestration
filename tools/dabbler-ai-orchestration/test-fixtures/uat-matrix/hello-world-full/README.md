# hello-world-full

A trivial static hello-world page used as the **Full-tier control
project** in the Dabbler UAT fixture matrix. The interesting content is
under `docs/session-sets/` — see `../README.md` for the row inventory.

Open `index.html` in a browser to see the page; `js/hello.js` renders
the greeting.
