# telemetry / dabbler-access-harvester — supporting (confounded)

Awaiting the first pilot run. `dabbler-access-harvester` is a **supporting** signal
— pilot set `019-...-dotnet-tool-packaging` (`.csproj` / CI / docs). Its diffs are
**small / packaging**, which is snippet-fittable and favors `push` for the wrong
reason, so it is **confounded** relative to the platform lead (Validity Threat 1).

Each build session drops a `<session>/` subdirectory here with its frozen
`matrix-run/` artifacts + `metadata.json` (`includedInFindingPower=true`,
`diffClass=packaging-small`). Contract: [`../README.md`](../README.md).
