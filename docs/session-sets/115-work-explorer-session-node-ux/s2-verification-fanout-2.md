**VERIFIED** — I tried to break the session-row activation path across the changed source, menu gates, shared heading parser, and tests. The implementation reuses `openSpec`, reveals the requested session when locatable, degrades to opening the real spec at the top when not, keeps session context menus absent, and the relevant Layer 2/Layer 3 checks pass; I found no Critical/Major issues or material nits.