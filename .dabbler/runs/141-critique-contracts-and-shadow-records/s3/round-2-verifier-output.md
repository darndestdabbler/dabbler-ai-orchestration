**ISSUES FOUND**

Prior findings re-check: the registered `no-shell-out` string-literal quote path now fails, and same-check `blocked` → `pass` is now refused. The blocking defect below is new in the fix delta.

- **Issue 1:** `record_worker_result` accepts worker results for check IDs that are not present in `checks.json`, so the check-derived AST contract is silently disabled and an unregistered “narrower” check can be recorded as passing.
  - **Category:** Correctness
  - **Severity:** Major
  - **Evidence paths:** `ai_router/evidence.py:918`, `ai_router/evidence.py:923`, `ai_router/evidence.py:885`, `tests/test_critique_contracts.py:307`, `tests/fixtures/critique-roundtrip/checks.json:1`
  - **Failure scenario:** A worker emits a typo or fabricated `check_id` such as `no-shell-out-in-render`. This is probable because the new test itself uses that unregistered ID as the allowed ladder exit. `record_worker_result` looks up the check, gets `None`, verifies with no required AST kinds, and appends the result anyway, leaving the ledger with evidence for a check the framework never registered or bounded.
  - **Acceptance criterion:** `JUDGMENT - record_worker_result must refuse a worker result whose check_id is absent from checks.json for that change_id before appending anything; the narrower-check pass path must first register that narrower check.`
  - **Details:** **Violation:** the implementation claims results are verified “against the check it answers,” and the plan requires quote AST kind requirements to come from the check, not the worker row. **Impact:** the ledger can contain passing results for non-existent checks, and the quote-contract protection turns off whenever check lookup misses, which undermines the core artifact contract and would change a reasonable merge decision. **Evidence:** `record_worker_result` uses `next(..., None)` for the check lookup and passes that `None` into `verify_worker_result`; `contract_ast_kinds(None)` yields no required kinds. The test then records `no-shell-out-in-render` even though the fixture defines only `no-shell-out`.