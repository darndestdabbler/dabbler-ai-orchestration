ISSUES FOUND

- **Issue 1:** The required single `evidence` git seam was not implemented; git operations remain rooted in `journal` and the unchanged `ledger` bypass remains.
  - **Category:** Completeness
  - **Severity:** Major
  - **Evidence paths:** `ai_router/evidence.py, ai_router/journal.py, ai_router/ledger.py, ai_router/runcli.py, ai_router/verifyjob.py`
  - **Failure scenario:** Normal session lifecycles exercise journal and ledger git operations. Because those operations do not route through `evidence.run_git`, any instrumentation, error handling, or process-spawn optimization applied at the promised seam will omit ordinary lifecycle traffic. This is probable rather than hypothetical because ledger and journal operations are part of every normal session.
  - **Acceptance criterion:** JUDGMENT - `evidence.run_git` must be the sole router function that spawns git, all direct git subprocess calls in `journal.py` and `ledger.py` must route through it, and `runcli.py`, `verifyjob.py`, and `workflow.py` must import the tree helpers from `evidence`.
  - **Details:** **Violation:** The plan requires: “Route the remaining direct `["git", "-C", …]` calls in `journal.py` and `ledger.py` through `evidence.run_git` so one function is the only place the router spawns git,” and specifically requires `runcli.py`, `verifyjob.py`, and `workflow.py` to import the tree helpers from `evidence`. **Impact:** The central “one git seam” deliverable is absent, leaving normal git traffic outside the promised control point and preserving the maintenance/performance divergence this session was intended to remove; this should block merging the session as complete. **Evidence:** `evidence.py` deletes its implementations and merely re-exports `run_git` and the tree helpers from `journal`; `journal.py` becomes their implementation location; `runcli.py` and `verifyjob.py` import them directly from `journal`; and `ledger.py` has no change despite the explicitly identified direct git calls. The correct implementation must make `evidence` the operative seam rather than an alias for the `journal` seam and must remove the remaining ledger bypass.