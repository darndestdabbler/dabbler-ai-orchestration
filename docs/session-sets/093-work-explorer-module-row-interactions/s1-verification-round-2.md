VERIFIED — The round-1 fallback defect is correctly fixed with a `planPath !== null` guard and a regression test that actually injects `planExists: true`. The host resolution, cross-root OR merge, exhaustive child-state derivation, persistent rendering for all module kinds, four-level ARIA hierarchy, collapse wiring, and Layer 2/3 pins are consistent with the settled Session 1 contract.